---
title: CMFCToolBarButton – třída
ms.date: 11/04/2016
f1_keywords:
- CMFCToolBarButton
- AFXTOOLBARBUTTON/CMFCToolBarButton
- AFXTOOLBARBUTTON/CMFCToolBarButton::CMFCToolBarButton
- AFXTOOLBARBUTTON/CMFCToolBarButton::CanBeDropped
- AFXTOOLBARBUTTON/CMFCToolBarButton::CanBeStored
- AFXTOOLBARBUTTON/CMFCToolBarButton::CanBeStretched
- AFXTOOLBARBUTTON/CMFCToolBarButton::CompareWith
- AFXTOOLBARBUTTON/CMFCToolBarButton::CopyFrom
- AFXTOOLBARBUTTON/CMFCToolBarButton::CreateFromOleData
- AFXTOOLBARBUTTON/CMFCToolBarButton::EnableWindow
- AFXTOOLBARBUTTON/CMFCToolBarButton::ExportToMenuButton
- AFXTOOLBARBUTTON/CMFCToolBarButton::GetClipboardFormat
- AFXTOOLBARBUTTON/CMFCToolBarButton::GetHwnd
- AFXTOOLBARBUTTON/CMFCToolBarButton::GetImage
- AFXTOOLBARBUTTON/CMFCToolBarButton::GetInvalidateRect
- AFXTOOLBARBUTTON/CMFCToolBarButton::GetParentWnd
- AFXTOOLBARBUTTON/CMFCToolBarButton::GetProtectedCommands
- AFXTOOLBARBUTTON/CMFCToolBarButton::GetTextSize
- AFXTOOLBARBUTTON/CMFCToolBarButton::HasFocus
- AFXTOOLBARBUTTON/CMFCToolBarButton::HaveHotBorder
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsDrawImage
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsDrawText
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsDroppedDown
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsEditable
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsExtraSize
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsFirstInGroup
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsHidden
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsHorizontal
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsLastInGroup
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsLocked
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsOwnerOf
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsVisible
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsWindowVisible
- AFXTOOLBARBUTTON/CMFCToolBarButton::NotifyCommand
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnAddToCustomizePage
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnBeforeDrag
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnBeforeDrop
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnCalculateSize
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnCancelMode
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnChangeParentWnd
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnClick
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnClickUp
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnContextHelp
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnCtlColor
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnCustomizeMenu
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnDblClk
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnDraw
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnDrawOnCustomizeList
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnGetCustomToolTipText
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnGlobalFontsChanged
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnMove
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnShow
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnSize
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnToolHitTest
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnUpdateToolTip
- AFXTOOLBARBUTTON/CMFCToolBarButton::PrepareDrag
- AFXTOOLBARBUTTON/CMFCToolBarButton::Rect
- AFXTOOLBARBUTTON/CMFCToolBarButton::ResetImageToDefault
- AFXTOOLBARBUTTON/CMFCToolBarButton::SaveBarState
- AFXTOOLBARBUTTON/CMFCToolBarButton::Serialize
- AFXTOOLBARBUTTON/CMFCToolBarButton::SetACCData
- AFXTOOLBARBUTTON/CMFCToolBarButton::SetClipboardFormatName
- AFXTOOLBARBUTTON/CMFCToolBarButton::SetImage
- AFXTOOLBARBUTTON/CMFCToolBarButton::SetProtectedCommands
- AFXTOOLBARBUTTON/CMFCToolBarButton::SetRadio
- AFXTOOLBARBUTTON/CMFCToolBarButton::SetRect
- AFXTOOLBARBUTTON/CMFCToolBarButton::SetStyle
- AFXTOOLBARBUTTON/CMFCToolBarButton::SetVisible
- AFXTOOLBARBUTTON/CMFCToolBarButton::Show
- AFXTOOLBARBUTTON/CMFCToolBarButton::m_bImage
- AFXTOOLBARBUTTON/CMFCToolBarButton::m_bText
- AFXTOOLBARBUTTON/CMFCToolBarButton::m_bTextBelow
- AFXTOOLBARBUTTON/CMFCToolBarButton::m_bUserButton
- AFXTOOLBARBUTTON/CMFCToolBarButton::m_bWholeText
- AFXTOOLBARBUTTON/CMFCToolBarButton::m_bWrap
- AFXTOOLBARBUTTON/CMFCToolBarButton::m_bWrapText
- AFXTOOLBARBUTTON/CMFCToolBarButton::m_nID
- AFXTOOLBARBUTTON/CMFCToolBarButton::m_nStyle
- AFXTOOLBARBUTTON/CMFCToolBarButton::m_strText
helpviewer_keywords:
- CMFCToolBarButton [MFC], CMFCToolBarButton
- CMFCToolBarButton [MFC], CanBeDropped
- CMFCToolBarButton [MFC], CanBeStored
- CMFCToolBarButton [MFC], CanBeStretched
- CMFCToolBarButton [MFC], CompareWith
- CMFCToolBarButton [MFC], CopyFrom
- CMFCToolBarButton [MFC], CreateFromOleData
- CMFCToolBarButton [MFC], EnableWindow
- CMFCToolBarButton [MFC], ExportToMenuButton
- CMFCToolBarButton [MFC], GetClipboardFormat
- CMFCToolBarButton [MFC], GetHwnd
- CMFCToolBarButton [MFC], GetImage
- CMFCToolBarButton [MFC], GetInvalidateRect
- CMFCToolBarButton [MFC], GetParentWnd
- CMFCToolBarButton [MFC], GetProtectedCommands
- CMFCToolBarButton [MFC], GetTextSize
- CMFCToolBarButton [MFC], HasFocus
- CMFCToolBarButton [MFC], HaveHotBorder
- CMFCToolBarButton [MFC], IsDrawImage
- CMFCToolBarButton [MFC], IsDrawText
- CMFCToolBarButton [MFC], IsDroppedDown
- CMFCToolBarButton [MFC], IsEditable
- CMFCToolBarButton [MFC], IsExtraSize
- CMFCToolBarButton [MFC], IsFirstInGroup
- CMFCToolBarButton [MFC], IsHidden
- CMFCToolBarButton [MFC], IsHorizontal
- CMFCToolBarButton [MFC], IsLastInGroup
- CMFCToolBarButton [MFC], IsLocked
- CMFCToolBarButton [MFC], IsOwnerOf
- CMFCToolBarButton [MFC], IsVisible
- CMFCToolBarButton [MFC], IsWindowVisible
- CMFCToolBarButton [MFC], NotifyCommand
- CMFCToolBarButton [MFC], OnAddToCustomizePage
- CMFCToolBarButton [MFC], OnBeforeDrag
- CMFCToolBarButton [MFC], OnBeforeDrop
- CMFCToolBarButton [MFC], OnCalculateSize
- CMFCToolBarButton [MFC], OnCancelMode
- CMFCToolBarButton [MFC], OnChangeParentWnd
- CMFCToolBarButton [MFC], OnClick
- CMFCToolBarButton [MFC], OnClickUp
- CMFCToolBarButton [MFC], OnContextHelp
- CMFCToolBarButton [MFC], OnCtlColor
- CMFCToolBarButton [MFC], OnCustomizeMenu
- CMFCToolBarButton [MFC], OnDblClk
- CMFCToolBarButton [MFC], OnDraw
- CMFCToolBarButton [MFC], OnDrawOnCustomizeList
- CMFCToolBarButton [MFC], OnGetCustomToolTipText
- CMFCToolBarButton [MFC], OnGlobalFontsChanged
- CMFCToolBarButton [MFC], OnMove
- CMFCToolBarButton [MFC], OnShow
- CMFCToolBarButton [MFC], OnSize
- CMFCToolBarButton [MFC], OnToolHitTest
- CMFCToolBarButton [MFC], OnUpdateToolTip
- CMFCToolBarButton [MFC], PrepareDrag
- CMFCToolBarButton [MFC], Rect
- CMFCToolBarButton [MFC], ResetImageToDefault
- CMFCToolBarButton [MFC], SaveBarState
- CMFCToolBarButton [MFC], Serialize
- CMFCToolBarButton [MFC], SetACCData
- CMFCToolBarButton [MFC], SetClipboardFormatName
- CMFCToolBarButton [MFC], SetImage
- CMFCToolBarButton [MFC], SetProtectedCommands
- CMFCToolBarButton [MFC], SetRadio
- CMFCToolBarButton [MFC], SetRect
- CMFCToolBarButton [MFC], SetStyle
- CMFCToolBarButton [MFC], SetVisible
- CMFCToolBarButton [MFC], Show
- CMFCToolBarButton [MFC], m_bImage
- CMFCToolBarButton [MFC], m_bText
- CMFCToolBarButton [MFC], m_bTextBelow
- CMFCToolBarButton [MFC], m_bUserButton
- CMFCToolBarButton [MFC], m_bWholeText
- CMFCToolBarButton [MFC], m_bWrap
- CMFCToolBarButton [MFC], m_bWrapText
- CMFCToolBarButton [MFC], m_nID
- CMFCToolBarButton [MFC], m_nStyle
- CMFCToolBarButton [MFC], m_strText
ms.assetid: 8a6ecffb-86b0-4f5c-8211-a9146b463efd
ms.openlocfilehash: 0793c1cecf07ddab8de93e9e5a2bc3af3513be97
ms.sourcegitcommit: fcb48824f9ca24b1f8bd37d647a4d592de1cc925
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 08/15/2019
ms.locfileid: "69504861"
---
# <a name="cmfctoolbarbutton-class"></a>CMFCToolBarButton – třída

Poskytuje funkce tlačítek pro panely nástrojů.

## <a name="syntax"></a>Syntaxe

```
class CMFCToolBarButton : public CObject
```

## <a name="members"></a>Členové

### <a name="public-constructors"></a>Veřejné konstruktory

|Name|Popis|
|----------|-----------------|
|[CMFCToolBarButton::CMFCToolBarButton](#cmfctoolbarbutton)|Vytvoří a inicializuje `CMFCToolBarButton` objekt.|
|`CMFCToolBarButton::~CMFCToolBarButton`|Destruktor.|

### <a name="public-methods"></a>Veřejné metody

|Name|Popis|
|----------|-----------------|
|[CMFCToolBarButton::CanBeDropped](#canbedropped)|Určuje, zda může uživatel umístit tlačítko na panelu nástrojů nebo v nabídce během přizpůsobení.|
|[CMFCToolBarButton::CanBeStored](#canbestored)|Určuje, zda lze tlačítko Uložit.|
|[CMFCToolBarButton::CanBeStretched](#canbestretched)|Určuje, zda může uživatel roztáhnout tlačítko během přizpůsobení.|
|[CMFCToolBarButton::CompareWith](#comparewith)|Porovná tuto instanci se zadaným `CMFCToolBarButton` objektem.|
|[CMFCToolBarButton::CopyFrom](#copyfrom)|Zkopíruje vlastnosti jiného tlačítka panelu nástrojů na aktuální tlačítko.|
|[CMFCToolBarButton::CreateFromOleData](#createfromoledata)|Vytvoří objekt ze zadaného `COleDataObject` objektu. `CMFCToolBarButton`|
|`CMFCToolBarButton::CreateObject`|Používá se rozhraním k vytvoření dynamické instance tohoto typu třídy.|
|[CMFCToolBarButton::EnableWindow](#enablewindow)|Povolí nebo zakáže vstup z klávesnice a myši.|
|[CMFCToolBarButton::ExportToMenuButton](#exporttomenubutton)|Kopíruje text z tlačítka panelu nástrojů do nabídky.|
|[CMFCToolBarButton::GetClipboardFormat](#getclipboardformat)|Načte globální formát schránky pro aplikaci.|
|[CMFCToolBarButton::GetHwnd](#gethwnd)|Načte popisovač okna, který je přidružený k tlačítku panelu nástrojů.|
|[CMFCToolBarButton::GetImage](#getimage)|Načte index obrázku tlačítka.|
|[CMFCToolBarButton::GetInvalidateRect](#getinvalidaterect)|Načte oblast klientské oblasti tlačítka, které musí být překresleny.|
|[CMFCToolBarButton::GetParentWnd](#getparentwnd)|Načte nadřazené okno tlačítka.|
|[CMFCToolBarButton::GetProtectedCommands](#getprotectedcommands)|Načte seznam příkazů, které uživatel nemůže přizpůsobit.|
|[CMFCToolBarButton::GetTextSize](#gettextsize)|Načte velikost textu tlačítka.|
|[CMFCToolBarButton::HasFocus](#hasfocus)|Určuje, zda má tlačítko aktuální vstupní fokus.|
|[CMFCToolBarButton::HaveHotBorder](#havehotborder)|Určuje, zda je zobrazeno ohraničení tlačítka, když uživatel vybere tlačítko.|
|[CMFCToolBarButton::IsDrawImage](#isdrawimage)|Určuje, zda je na tlačítku zobrazen obrázek.|
|[CMFCToolBarButton::IsDrawText](#isdrawtext)|Určuje, zda je na tlačítku zobrazen textový popisek.|
|[CMFCToolBarButton::IsDroppedDown](#isdroppeddown)|Určuje, zda tlačítko zobrazí podnabídku.|
|[CMFCToolBarButton::IsEditable](#iseditable)|Určuje, zda lze tlačítko přizpůsobit.|
|[CMFCToolBarButton::IsExtraSize](#isextrasize)|Určuje, zda lze tlačítko Zobrazit s rozšířeným ohraničením.|
|[CMFCToolBarButton::IsFirstInGroup](#isfirstingroup)|Určuje, zda je tlačítko v první pozici ve své skupině tlačítek.|
|[CMFCToolBarButton::IsHidden](#ishidden)|Určuje, zda je tlačítko skryté.|
|[CMFCToolBarButton::IsHorizontal](#ishorizontal)|Určuje, zda je tlačítko umístěno na vodorovném panelu nástrojů.|
|[CMFCToolBarButton::IsLastInGroup](#islastingroup)|Určuje, zda je tlačítko ve své skupině tlačítek ve své poslední pozici.|
|[CMFCToolBarButton::IsLocked](#islocked)|Určuje, zda je tlačítko na uzamčeném panelu nástrojů (nepřizpůsobitelný).|
|[CMFCToolBarButton::IsOwnerOf](#isownerof)|Určuje, zda je tlačítko vlastníkem zadaného popisovače okna.|
|[CMFCToolBarButton::IsVisible](#isvisible)|Určuje, zda je tlačítko panelu nástrojů viditelné.|
|[CMFCToolBarButton::IsWindowVisible](#iswindowvisible)|Určuje, zda je podkladové okno tlačítka viditelné.|
|[CMFCToolBarButton::NotifyCommand](#notifycommand)|Určuje, zda tlačítko zpracovává zprávu [WM_COMMAND](/windows/win32/menurc/wm-command) .|
|[CMFCToolBarButton::OnAddToCustomizePage](#onaddtocustomizepage)|Volá se rozhraním, když se přidá tlačítko do dialogového okna **přizpůsobit** .|
|[CMFCToolBarButton::OnBeforeDrag](#onbeforedrag)|Určuje, zda lze tlačítko přetáhnout.|
|[CMFCToolBarButton::OnBeforeDrop](#onbeforedrop)|Určuje, zda uživatel může tlačítko vyřadit na cílový panel nástrojů.|
|[CMFCToolBarButton::OnCalculateSize](#oncalculatesize)|Volá se rozhraním, aby se počítala velikost tlačítka pro zadaný kontext zařízení a stav docking.|
|[CMFCToolBarButton::OnCancelMode](#oncancelmode)|Volá se rozhraním, aby se zpracovala zpráva [WM_CANCELMODE](/windows/win32/winmsg/wm-cancelmode) .|
|[CMFCToolBarButton::OnChangeParentWnd](#onchangeparentwnd)|Volá se rozhraním, když se na nový panel nástrojů Vloží tlačítko.|
|[CMFCToolBarButton::OnClick](#onclick)|Volá se rozhraním, když uživatel klikne na tlačítko myši.|
|[CMFCToolBarButton::OnClickUp](#onclickup)|Volá se rozhraním, když uživatel uvolní tlačítko myši.|
|[CMFCToolBarButton::OnContextHelp](#oncontexthelp)|Volá se rozhraním, když nadřazený panel nástrojů zpracovává zprávu WM_HELPHITTEST.|
|[CMFCToolBarButton::OnCtlColor](#onctlcolor)|Volá se rozhraním, když nadřazený panel nástrojů zpracovává zprávu WM_CTLCOLOR.|
|[CMFCToolBarButton::OnCustomizeMenu](#oncustomizemenu)|Umožňuje tlačítku změnit poskytnutou nabídku, když aplikace zobrazuje místní nabídku na nadřazeném panelu nástrojů.|
|[CMFCToolBarButton::OnDblClk](#ondblclk)|Volá se rozhraním, když nadřazený panel nástrojů zpracovává zprávu [WM_LBUTTONDBLCLK](/windows/win32/inputdev/wm-lbuttondblclk) .|
|[CMFCToolBarButton::OnDraw](#ondraw)|Volá se rozhraním, aby se vykreslilo tlačítko pomocí zadaných stylů a možností.|
|[CMFCToolBarButton::OnDrawOnCustomizeList](#ondrawoncustomizelist)|Volá se rozhraním, aby se nakreslilo tlačítko v podokně **příkazy** dialogového okna **přizpůsobit** .|
|[CMFCToolBarButton::OnGetCustomToolTipText](#ongetcustomtooltiptext)|Volá se rozhraním, aby se získal text vlastního popisu tlačítka.|
|[CMFCToolBarButton::OnGlobalFontsChanged](#onglobalfontschanged)|Volá se rozhraním, když se změní globální písmo.|
|[CMFCToolBarButton::OnMove](#onmove)|Volá se rozhraním, když se přesune nadřazený panel nástrojů.|
|[CMFCToolBarButton::OnShow](#onshow)|Volá se rozhraním, když se tlačítko zobrazí jako viditelné nebo neviditelné.|
|[CMFCToolBarButton::-size](#onsize)|Volá se rozhraním, když nadřazený panel nástrojů změní svou velikost nebo polohu a tato změna vyžaduje změnu velikosti tlačítka.|
|[CMFCToolBarButton::OnToolHitTest](#ontoolhittest)|Volá se rozhraním, když nadřazený panel nástrojů musí určit, jestli je bod v ohraničujícím obdélníku tlačítka.|
|[CMFCToolBarButton::OnUpdateToolTip](#onupdatetooltip)|Volá se rozhraním, když nadřazený panel nástrojů aktualizuje svůj text popisku.|
|[CMFCToolBarButton::PrepareDrag](#preparedrag)|Volá se rozhraním, když má tlačítko k provedení operace přetažení.|
|[CMFCToolBarButton:: Rect](#rect)|Načte ohraničující obdélník tlačítka.|
|[CMFCToolBarButton::ResetImageToDefault](#resetimagetodefault)|Nastaví výchozí hodnotu na obrázek, který je přidružený k tlačítku.|
|[CMFCToolBarButton::SaveBarState](#savebarstate)|Uloží stav tlačítka panelu nástrojů.|
|[CMFCToolBarButton:: serializovat](#serialize)|Přečte tento objekt z archivu nebo ho zapíše do archivu. (Overrides [CObject:: serializovat](../../mfc/reference/cobject-class.md#serialize).)|
|[CMFCToolBarButton::SetACCData](#setaccdata)|Naplní poskytnutý `CAccessibilityData` objekt daty přístupnosti z tlačítka panelu nástrojů.|
|[CMFCToolBarButton::SetClipboardFormatName](#setclipboardformatname)|Přejmenuje globální formát schránky.|
|[CMFCToolBarButton::SetImage](#setimage)|Nastaví index obrázku tlačítka.|
|[CMFCToolBarButton::SetProtectedCommands](#setprotectedcommands)|Nastaví seznam příkazů, které uživatel nemůže přizpůsobit.|
|[CMFCToolBarButton::SetRadio](#setradio)|Volá se rozhraním, když se změní stav zaškrtnutí tlačítka.|
|[CMFCToolBarButton::SetRect](#setrect)|Nastaví ohraničující obdélník tlačítka.|
|[CMFCToolBarButton:: SetStyle](#setstyle)|Nastaví styl tlačítka.|
|[CMFCToolBarButton:: SetVisible](#setvisible)|Určuje, zda je tlačítko viditelné.|
|[CMFCToolBarButton:: show](#show)|Zobrazí nebo skryje tlačítko.|

### <a name="data-members"></a>Datové členy

|Name|Popis|
|----------|-----------------|
|[CMFCToolBarButton::m_bImage](#m_bimage)|Určuje, zda je na tlačítku zobrazen obrázek.|
|[CMFCToolBarButton::m_bText](#m_btext)|Určuje, zda je na tlačítku zobrazen textový popisek.|
|[CMFCToolBarButton::m_bTextBelow](#m_btextbelow)|Určuje, zda je textový popisek zobrazen pod obrázkem na tlačítku.|
|[CMFCToolBarButton::m_bUserButton](#m_buserbutton)|Určuje, zda má tlačítko uživatelem definovanou bitovou kopii.|
|[CMFCToolBarButton::m_bWholeText](#m_bwholetext)|Určuje, zda tlačítko zobrazí úplný text popisku, i když se nevejde do ohraničujícího obdélníku.|
|[CMFCToolBarButton::m_bWrap](#m_bwrap)|Určuje, zda bude tlačítko vedle oddělovače vloženo na další řádek.|
|[CMFCToolBarButton::m_bWrapText](#m_bwraptext)|Určuje, zda jsou povoleny víceřádkové textové popisky.|
|[CMFCToolBarButton::m_nID](#m_nid)|ID příkazu tlačítka|
|[CMFCToolBarButton::m_nStyle](#m_nstyle)|Styl tlačítka|
|[CMFCToolBarButton::m_strText](#m_strtext)|Textový popisek tlačítka|

## <a name="remarks"></a>Poznámky

`CMFCToolbarButton` Objekt je ovládací prvek, který se nachází na panelu nástrojů. Chování je podobné jako běžné tlačítko. Tomuto objektu můžete přiřadit obrázek a textový popisek. Tlačítko panelu nástrojů může mít také ID příkazu. Když uživatel klikne na tlačítko panelu nástrojů, rozhraní provede příkaz, který toto ID určuje.

Tlačítka panelu nástrojů lze obvykle přizpůsobit: uživatel může přetahovat tlačítka z jednoho panelu nástrojů na jiný a kopírovat, vkládat, odstraňovat a upravovat textové popisky a obrázky. Chcete-li uživateli zabránit v přizpůsobení panelu nástrojů, můžete panel nástrojů uzamknout jedním ze dvou způsobů. Buď nastavte `bLocked` příznak na hodnotu true při volání [CMFCToolBar:: LoadToolBar](../../mfc/reference/cmfctoolbar-class.md#loadtoolbar)nebo přidejte ID příkazu individuálního tlačítka do globálního seznamu chráněných příkazů pomocí metody [CMFCToolBarButton:: SetProtectedCommands](#setprotectedcommands) .

`CMFCToolBarButton`objekty zobrazují obrázky z globálních kolekcí imagí panelů nástrojů v aplikaci. Tyto kolekce jsou spravovány nadřazeným panelem nástrojů [CMFCToolBar třídy](../../mfc/reference/cmfctoolbar-class.md). Další informace naleznete v tématu [Třída CMFCToolBarImages](../../mfc/reference/cmfctoolbarimages-class.md).

Když uživatel klikne na tlačítko panelu nástrojů, jeho nadřazený panel nástrojů zpracuje zprávu myši a na tlačítko sdělí příslušné akce. Pokud má tlačítko platné ID příkazu, nadřazený panel nástrojů pošle zprávu WM_COMMAND nadřazenému snímku.

Třída je základní třídou pro jiné třídy tlačítek panelu nástrojů, jako je například [Třída CMFCToolBarMenuButton](../../mfc/reference/cmfctoolbarmenubutton-class.md), Třída CMFCToolBarEditBoxButton a [Třída CMFCToolBarComboBoxButton](../../mfc/reference/cmfctoolbarcomboboxbutton-class.md). [](../../mfc/reference/cmfctoolbareditboxbutton-class.md) `CMFCToolBarButton`

## <a name="example"></a>Příklad

Následující příklad ukazuje, jak nakonfigurovat `CMFCToolBarButton` objekt pomocí různých metod `CMFCToolBarButton` ve třídě. Příklad ukazuje, jak povolit vstup myši a klávesnice, nastavit index obrázku tlačítka, nastavit ohraničující obdélník tlačítka a nastavit tlačítko jako viditelné. Tento fragment kódu je součástí [ukázky ovládacího prvku karta](../../overview/visual-cpp-samples.md).

[!code-cpp[NVC_MFC_TabControl#1](../../mfc/reference/codesnippet/cpp/cmfctoolbarbutton-class_1.cpp)]
[!code-cpp[NVC_MFC_TabControl#2](../../mfc/reference/codesnippet/cpp/cmfctoolbarbutton-class_2.cpp)]

## <a name="inheritance-hierarchy"></a>Hierarchie dědičnosti

[CObject](../../mfc/reference/cobject-class.md)

[CMFCToolBarButton](../../mfc/reference/cmfctoolbarbutton-class.md)

## <a name="requirements"></a>Požadavky

**Záhlaví:** afxtoolbarbutton. h

##  <a name="canbedropped"></a>CMFCToolBarButton::CanBeDropped

Určuje, zda může uživatel umístit tlačítko na panelu nástrojů nebo v nabídce během přizpůsobení.

```
virtual BOOL CanBeDropped(CMFCToolBar* pToolbar);
```

### <a name="parameters"></a>Parametry

*pToolbar*<br/>
pro Nepoužívané.

### <a name="return-value"></a>Návratová hodnota

Tato metoda vrátí hodnotu TRUE.

### <a name="remarks"></a>Poznámky

Ve výchozím nastavení je možné vyřadit tlačítko panelu nástrojů na všechny přizpůsobitelné panely nástrojů (tj. neuzamknutý).

Výchozí implementace této metody vrátí hodnotu TRUE. Tuto metodu přepište a vraťte hodnotu FALSE, pokud chcete uživateli zabránit v přemístění tlačítka.

##  <a name="canbestored"></a>CMFCToolBarButton::CanBeStored

Určuje, zda lze tlačítko Uložit.

```
virtual BOOL CanBeStored() const;
```

### <a name="return-value"></a>Návratová hodnota

Tato metoda vrátí hodnotu TRUE.

### <a name="remarks"></a>Poznámky

Rozhraní používá tuto metodu k určení, zda se může tlačítko účastnit operace přetažení.

Výchozí implementace vrátí hodnotu TRUE. Tuto metodu přepište, pokud tlačítko nelze uložit jako součást operace přetažení. Další informace o operacích přetažení naleznete v tématu přetažení [(OLE)](../../mfc/drag-and-drop-ole.md).

##  <a name="canbestretched"></a>CMFCToolBarButton::CanBeStretched

Určuje, zda může uživatel roztáhnout tlačítko během přizpůsobení.

```
virtual BOOL CanBeStretched() const;
```

### <a name="return-value"></a>Návratová hodnota

Tato metoda vrátí hodnotu FALSE.

### <a name="remarks"></a>Poznámky

Tato metoda je používána rozhraním k určení, zda lze tlačítko roztáhnout do režimu přizpůsobení.

Výchozí implementace této metody vrátí hodnotu FALSE. Tuto metodu přepište, pokud chcete vrátit hodnotu TRUE pro ovládací prvek proměnné šířky, jako je například pole se seznamem nebo posuvník.

Další informace o režimu přizpůsobení naleznete v tématu [CMFCToolBar:: SetCustomizeMode](../../mfc/reference/cmfctoolbar-class.md#setcustomizemode).

##  <a name="cmfctoolbarbutton"></a>CMFCToolBarButton::CMFCToolBarButton

Vytvoří a inicializuje `CMFCToolBarButton` objekt.

```
CMFCToolBarButton(
    UINT uiID,
    int iImage,
    LPCTSTR lpszText=NULL,
    BOOL bUserButton=FALSE,
    BOOL bLocked=FALSE);
```

### <a name="parameters"></a>Parametry

*uiID*<br/>
pro ID příkazu tlačítka

*iImage*<br/>
pro Index obrázku tlačítka v kolekci obrázků

*lpszText*<br/>
pro Textový popisek tlačítka Může mít hodnotu NULL.

*bUserButton*<br/>
pro Logická hodnota, která určuje, zda je tlačítko definováno uživatelem. Pokud má tento parametr hodnotu TRUE, je tlačítko definováno uživatelem. V opačném případě je obrázek tlačítka načten z prostředku.

*Blokované*<br/>
pro Logická hodnota, která určuje, zda lze tlačítko přizpůsobit. Pokud má tento parametr hodnotu TRUE, tlačítko nejde přizpůsobit. V opačném případě lze tlačítko přizpůsobit.

##  <a name="comparewith"></a>CMFCToolBarButton:: compareWith –

Porovná tuto instanci se zadaným `CMFCToolBarButton` objektem.

```
virtual BOOL CompareWith(const CMFCToolBarButton& other) const;
```

### <a name="parameters"></a>Parametry

*jiná*<br/>
pro Odkaz na objekt, který se má porovnat s touto instancí

### <a name="return-value"></a>Návratová hodnota

Nenulové, pokud se zadaný objekt rovná hodnotě této instance; v opačném případě 0.

### <a name="remarks"></a>Poznámky

Výchozí implementace určuje, zda ID příkazu zadaného objektu odpovídá ID příkazu této instance. Tuto metodu přepište, pokud je nutné provést další zpracování, aby `CMFCToolBarButton` bylo možné zjistit, zda jsou dva objekty stejné.

##  <a name="copyfrom"></a>CMFCToolBarButton::CopyFrom

Zkopíruje vlastnosti jiného tlačítka panelu nástrojů na aktuální tlačítko.

```
virtual void CopyFrom(const CMFCToolBarButton& src);
```

### <a name="parameters"></a>Parametry

*src*<br/>
pro Odkaz na zdrojové tlačítko, ze kterého se má kopírovat.

### <a name="remarks"></a>Poznámky

Voláním této metody zkopírujte další tlačítko panelu nástrojů na toto tlačítko panelu nástrojů.

##  <a name="createfromoledata"></a>  CMFCToolBarButton::CreateFromOleData

Vytvoří objekt ze zadaného `COleDataObject` objektu. `CMFCToolBarButton`

```
static CMFCToolBarButton* __stdcall CreateFromOleData(COleDataObject* pDataObject);
```

### <a name="parameters"></a>Parametry

*pDataObject*<br/>
pro Zdrojový datový objekt OLE.

### <a name="return-value"></a>Návratová hodnota

Vytvořený `CMFCToolBarButton` objekt.

### <a name="remarks"></a>Poznámky

Tato metoda je používána rozhraním k provádění přenosu dat v různých formátech. Například `CMFCOutlookBarPane::OnDragOver` metoda používá tuto metodu k provádění operací přetažení.

##  <a name="enablewindow"></a>CMFCToolBarButton::EnableWindow

Povolí nebo zakáže vstup z klávesnice a myši.

```
virtual void EnableWindow(BOOL bEnable = TRUE);
```

### <a name="parameters"></a>Parametry

*bEnable*<br/>
pro Nastavte tento parametr na hodnotu TRUE, pokud chcete vstup povolit, nebo na hodnotu FALSE, chcete-li zakázat vstup.

### <a name="remarks"></a>Poznámky

Tato metoda volá `EnableWindow` funkci pro povolení nebo zakázání vstupu. Další informace najdete v tématu [EnableWindow](/windows/win32/api/winuser/nf-winuser-enablewindow) v Windows SDK.

##  <a name="exporttomenubutton"></a>CMFCToolBarButton::ExportToMenuButton

Kopíruje text z tlačítka panelu nástrojů do nabídky.

```
virtual BOOL ExportToMenuButton(CMFCToolBarMenuButton& menuButton) const;
```

### <a name="parameters"></a>Parametry

*menuButton*<br/>
pro Odkaz na tlačítko cílové nabídky

### <a name="return-value"></a>Návratová hodnota

Tato metoda vrátí hodnotu TRUE.

### <a name="remarks"></a>Poznámky

Rozhraní volá tuto metodu pro zkopírování textu z tlačítka panelu nástrojů na tlačítko nabídky. Výchozí implementace kopíruje textový popisek tlačítka. Pokud je textový popisek prázdný, tato metoda zkopíruje text popisku tlačítka.

Výchozí implementace této metody vrátí hodnotu TRUE. Tuto metodu přepište, pokud chcete provést další akce, když rozhraní převede objekt, který je odvozen z [CMFCToolBarButton](../../mfc/reference/cmfctoolbarbutton-class.md) na tlačítko nabídky.

##  <a name="getclipboardformat"></a>CMFCToolBarButton::GetClipboardFormat

Načte globální formát schránky pro aplikaci.

```
static CLIPFORMAT __stdcall GetClipboardFormat();
```

### <a name="return-value"></a>Návratová hodnota

Globální hodnota CLIPFORMAT pro aplikaci

### <a name="remarks"></a>Poznámky

Rozhraní volá tuto metodu, aby získala formát schránky pro operace přenosu dat OLE. Například metoda [CMFCToolBarButton:: CreateFromOleData](#createfromoledata) používá tuto metodu ke kopírování dat ze zdrojového datového objektu OLE.

Tato metoda nastavuje globální hodnotu CLIPFORMAT při prvním volání této metody. Všechna následující volání této metody vracejí tuto hodnotu.

Chcete-li, aby mezi aplikacemi probíhaly operace přetažení, zavolejte metodu [CMFCToolBarButton:: SetClipboardFormatName](#setclipboardformatname) .

Další informace o schránkách v knihovně MFC naleznete v tématu [Clipboard](../../mfc/clipboard.md).

##  <a name="gethwnd"></a>CMFCToolBarButton:: GetHwnd

Načte popisovač okna, který je přidružený k tlačítku panelu nástrojů.

```
virtual HWND GetHwnd();
```

### <a name="return-value"></a>Návratová hodnota

Popisovač okna, který je přidružen k tlačítku panelu nástrojů nebo hodnotu NULL, pokud tlačítko na panelu nástrojů nemá žádný přidružený popisovač okna.

### <a name="remarks"></a>Poznámky

Výchozí implementace této metody vrací hodnotu NULL. Tuto metodu přepište, pokud chcete vrátit obslužnou rutinu vašeho konkrétního ovládacího prvku.

##  <a name="getimage"></a>CMFCToolBarButton:: GetImage

Načte index obrázku tlačítka.

```
int GetImage() const;
```

### <a name="return-value"></a>Návratová hodnota

Index obrázku přidruženého k tomuto tlačítku

### <a name="remarks"></a>Poznámky

Pokud má tlačítko uživatelem definovanou Image (to znamená, pokud *bUserButton* byl v konstruktoru true), vrácený index určuje obrázek v kolekci uživatelsky definovaných imagí (viz [CMFCToolBar:: GetUserImages](../../mfc/reference/cmfctoolbar-class.md#getuserimages)). V opačném případě index určuje obrázek v kolekci imagí načtených ze souboru prostředků (viz [CMFCToolBar::](../../mfc/reference/cmfctoolbar-class.md#getimages)getimages). Další informace o souborech prostředků naleznete v tématu [práce se soubory prostředků](../../windows/working-with-resource-files.md).

##  <a name="getinvalidaterect"></a>CMFCToolBarButton::GetInvalidateRect

Načte oblast klientské oblasti tlačítka, které musí být překresleny.

```
virtual const CRect GetInvalidateRect() const;
```

### <a name="return-value"></a>Návratová hodnota

`CRect` Objekt, který určuje oblast, která musí být překreslena.

### <a name="remarks"></a>Poznámky

Výchozí implementace této metody vrátí celou klientskou oblast. Tuto metodu přepište, pokud chcete překreslit jinou oblast.

##  <a name="getparentwnd"></a>CMFCToolBarButton::GetParentWnd

Načte nadřazené okno tlačítka.

```
CWnd* GetParentWnd() const;
```

### <a name="return-value"></a>Návratová hodnota

Nadřazené okno tlačítka

##  <a name="getprotectedcommands"></a>CMFCToolBarButton::GetProtectedCommands

Načte seznam příkazů, které uživatel nemůže přizpůsobit.

```
static const CList<UINT,UINT>& GetProtectedCommands();
```

### <a name="return-value"></a>Návratová hodnota

Seznam chráněných příkazů.

### <a name="remarks"></a>Poznámky

V režimu přizpůsobení rozhraní zakáže příkazy tlačítek na panelu nástrojů, které jsou chráněné. Uživatel nemůže provádět operace přetahování a úprav na zakázaných tlačítkách na panelu nástrojů.

Pro definování seznamu chráněných příkazů použijte metodu [CMFCToolBarButton:: SetProtectedCommands](#setprotectedcommands) .

##  <a name="gettextsize"></a>CMFCToolBarButton::GetTextSize

Načte velikost textu tlačítka.

```
SIZE GetTextSize() const;
```

### <a name="return-value"></a>Návratová hodnota

Objekt velikosti, který obsahuje velikost textu tlačítka v pixelech.

##  <a name="hasfocus"></a>CMFCToolBarButton::HasFocus

Určuje, zda má tlačítko aktuální vstupní fokus.

```
virtual BOOL HasFocus() const;
```

### <a name="return-value"></a>Návratová hodnota

Nenulové, pokud má tlačítko vstupní fokus; v opačném případě 0.

### <a name="remarks"></a>Poznámky

Výchozí implementace této metody vrátí nenulovou hodnotu, pokud má tlačítko vstupní fokus nebo se jedná o podřízené okno nebo následníka okna, které má fokus vstupu. Tuto funkci můžete přepsat, chcete-li toto chování přizpůsobit.

##  <a name="havehotborder"></a>CMFCToolBarButton::HaveHotBorder

Určuje, zda je zobrazeno ohraničení tlačítka, když uživatel vybere tlačítko.

```
virtual BOOL HaveHotBorder() const;
```

### <a name="return-value"></a>Návratová hodnota

Tato metoda vrátí hodnotu TRUE.

### <a name="remarks"></a>Poznámky

Rozhraní volá tuto metodu, aby určila, zda má tlačítko na panelu nástrojů zobrazit jeho ohraničení, když ho uživatel vybere.

Výchozí implementace vrátí hodnotu TRUE. Tuto metodu můžete pro přizpůsobení tohoto chování přepsat.

##  <a name="isdrawimage"></a>CMFCToolBarButton::IsDrawImage

Určuje, zda je na tlačítku zobrazen obrázek.

```
BOOL IsDrawImage() const;
```

### <a name="return-value"></a>Návratová hodnota

Nenulové, pokud je na tlačítku zobrazen obrázek; v opačném případě 0.

### <a name="remarks"></a>Poznámky

Tato metoda vrátí hodnotu FALSE, pokud tlačítko na panelu nástrojů nemá přidruženou bitovou kopii ( [CMFCToolBarButton:: GetImage](#getimage) vrátí hodnotu-1), nebo pokud [CMFCToolBarButton:: m_bImage](#m_bimage) je nastavena na hodnotu false.

##  <a name="isdrawtext"></a>CMFCToolBarButton::IsDrawText

Určuje, zda je na tlačítku zobrazen textový popisek.

```
BOOL IsDrawText() const;
```

### <a name="return-value"></a>Návratová hodnota

Nenulové, pokud je zobrazen textový popisek; v opačném případě 0.

### <a name="remarks"></a>Poznámky

Tato metoda vrátí hodnotu FALSE, pokud tlačítko na panelu nástrojů nemá přidružený textový popisek ( [CMFCToolBarButton:: m_strText](#m_strtext) je prázdný), nebo [CMFCToolBarButton:: m_bText](#m_btext) je nastaveno na false.

##  <a name="isdroppeddown"></a>CMFCToolBarButton::IsDroppedDown

Určuje, zda tlačítko zobrazí podnabídku.

```
virtual BOOL IsDroppedDown() const;
```

### <a name="return-value"></a>Návratová hodnota

Tato metoda vrátí hodnotu FALSE.

### <a name="remarks"></a>Poznámky

Výchozí implementace této metody vrátí hodnotu FALSE. Přepsáním této metody vrátíte hodnotu TRUE, pokud ovládací prvek zobrazí podnabídku.

##  <a name="iseditable"></a>CMFCToolBarButton::-upravitelné

Určuje, zda lze tlačítko přizpůsobit.

```
virtual BOOL IsEditable() const;
```

### <a name="return-value"></a>Návratová hodnota

Nenulové, pokud může být tlačítko přizpůsobeno uživatelem; v opačném případě 0.

### <a name="remarks"></a>Poznámky

Rozhraní volá tuto metodu, aby určila, jestli uživatel může přizpůsobit tlačítko panelu nástrojů pomocí operací přetažení nebo úprav.

Výchozí implementace vrátí hodnotu false, pokud je ID příkazu standardního příkazu (můžete to zjistit voláním `IsStandardCommand` funkce) nebo v případě, že je ID příkazu v seznamu chráněných příkazů. Další informace o chráněných příkazech naleznete v tématu [CMFCToolBarButton:: GetProtectedCommands](#getprotectedcommands) a [CMFCToolBarButton:: SetProtectedCommands](#setprotectedcommands).

Tuto metodu přepište, pokud chcete přizpůsobit její chování.

##  <a name="isextrasize"></a>CMFCToolBarButton::IsExtraSize

Určuje, zda lze tlačítko Zobrazit s rozšířeným ohraničením.

```
virtual BOOL IsExtraSize() const;
```

### <a name="return-value"></a>Návratová hodnota

Nenulové, pokud lze tlačítko panelu nástrojů zobrazit s rozšířeným ohraničením; v opačném případě 0.

### <a name="remarks"></a>Poznámky

Několik vzhledů používá pro ohraničení tlačítek panelu nástrojů dodatečnou velikost (například kulatá tlačítka).

Pokud uživatel přesune toto tlačítko z jednoho panelu nástrojů na jiný, rozhraní zavolá metodu [CMFCToolBarButton:: OnChangeParentWnd](#onchangeparentwnd) . Metoda [CMFCToolBarButton:: OnChangeParentWnd](#onchangeparentwnd) nastaví příznak extra Size na tento nový nadřazený panel nástrojů (Další informace naleznete v tématu [CMFCToolBar:: IsButtonExtraSizeAvailable](../../mfc/reference/cmfctoolbar-class.md#isbuttonextrasizeavailable)).

##  <a name="isfirstingroup"></a>CMFCToolBarButton::IsFirstInGroup

Určuje, zda je tlačítko v první pozici ve své skupině tlačítek.

```
virtual BOOL IsFirstInGroup() const;
```

### <a name="return-value"></a>Návratová hodnota

TRUE, pokud je tlačítko prvním tlačítkem ve své skupině tlačítek; v opačném případě FALSE.

### <a name="remarks"></a>Poznámky

Tato metoda definuje *skupinu tlačítek* jako sousední sadu tlačítek, která jsou umístěna na stejném řádku a jsou ohraničena oddělovači nebo ohraničením panelu nástrojů. Tato metoda vrátí hodnotu FALSE, pokud tlačítko panelu nástrojů odkazuje na tlačítko **přizpůsobit** . Další informace o tlačítku pro **přizpůsobení** naleznete v tématu [CMFCToolBar:: GetCustomizeButton](../../mfc/reference/cmfctoolbar-class.md#getcustomizebutton).

Voláním metody [CMFCToolBarButton:: IsLastInGroup](#islastingroup) určíte, zda je tlačítko ve své skupině tlačítek ve své poslední pozici.

##  <a name="ishidden"></a>CMFCToolBarButton:: Hidden

Určuje, zda je tlačítko skryté.

```
BOOL IsHidden() const;
```

### <a name="return-value"></a>Návratová hodnota

Nenulové, pokud je tlačítko skryté (neviditelné); v opačném případě 0.

### <a name="remarks"></a>Poznámky

Rozhraní volá tuto metodu, když je roztažen nadřazený panel nástrojů, aby bylo možné zjistit, zda je tlačítko panelu nástrojů viditelné.

Pokud nastavíte tlačítko jako neviditelné pomocí metody [CMFCToolBarButton:: SetVisible](#setvisible) , použijte [CMFCToolBarButton::-Visible](#isvisible) a určete, zda je tlačítko panelu nástrojů viditelné.

Ve výchozím nastavení jsou viditelná všechna tlačítka panelu nástrojů. Pomocí metody [CMFCToolBarButton:: show](#show) skryjte nebo zobrazte tlačítka panelu nástrojů.

##  <a name="ishorizontal"></a>CMFCToolBarButton::-Horizontal

Určuje, zda je tlačítko umístěno na vodorovném panelu nástrojů.

```
BOOL IsHorizontal() const;
```

### <a name="return-value"></a>Návratová hodnota

Nenulové, pokud je tlačítko panelu nástrojů umístěno na vodorovném panelu nástrojů; v opačném případě 0.

### <a name="remarks"></a>Poznámky

Rozhraní volá tuto metodu, aby určila rozložení tlačítek panelu nástrojů.

Tato metoda vrací `m_bHorz` datový člen. Výchozí hodnota `m_bHorz` datového členu je true. při každém volání metody [CMFCToolBarButton:: Draw](#ondraw) se resetuje.

##  <a name="islastingroup"></a>CMFCToolBarButton::IsLastInGroup

Určuje, zda je tlačítko ve své skupině tlačítek ve své poslední pozici.

```
virtual BOOL IsLastInGroup() const;
```

### <a name="return-value"></a>Návratová hodnota

TRUE, pokud je tlačítko posledním tlačítkem ve své skupině tlačítek; v opačném případě FALSE.

### <a name="remarks"></a>Poznámky

Tato metoda definuje *skupinu tlačítek* jako sousední sadu tlačítek, která jsou umístěna na stejném řádku a jsou ohraničena oddělovači nebo ohraničením panelu nástrojů. Tato metoda vrátí hodnotu false, pokud tlačítko panelu nástrojů nemá žádný nadřazený panel nástrojů nebo tlačítko panelu nástrojů odkazuje na tlačítko **přizpůsobit** . Další informace o tlačítku pro **přizpůsobení** naleznete v tématu [CMFCToolBar:: GetCustomizeButton](../../mfc/reference/cmfctoolbar-class.md#getcustomizebutton).

Voláním metody [CMFCToolBarButton:: IsFirstInGroup](#isfirstingroup) určíte, zda je tlačítko ve své skupině tlačítek ve své první pozici.

##  <a name="islocked"></a>CMFCToolBarButton::-lockd

Určuje, zda je tlačítko na uzamčeném panelu nástrojů (nepřizpůsobitelný).

```
BOOL IsLocked() const;
```

### <a name="return-value"></a>Návratová hodnota

Nenulové, pokud je tlačítko na uzamčeném panelu nástrojů; v opačném případě 0.

### <a name="remarks"></a>Poznámky

Rozhraní volá tuto metodu, aby určila, jestli uživatel může přizpůsobit tlačítko panelu nástrojů pomocí operací přetažení nebo úprav. Nastavte uzamčený atribut na nadřazeném panelu nástrojů pomocí metody [CMFCToolBar:: LoadToolBar](../../mfc/reference/cmfctoolbar-class.md#loadtoolbar) . Rozhraní předá hodnotu tohoto atributu konstruktoru každého tlačítka panelu nástrojů ( [CMFCToolBarButton](../../mfc/reference/cmfctoolbarbutton-class.md)), které vloží do nadřazeného panelu nástrojů.

##  <a name="isownerof"></a>CMFCToolBarButton::IsOwnerOf

Určuje, zda je tlačítko vlastníkem zadaného popisovače okna.

```
virtual BOOL IsOwnerOf(HWND hwnd);
```

### <a name="parameters"></a>Parametry

*HWND*<br/>
pro Popisovač okna.

### <a name="return-value"></a>Návratová hodnota

Nenulové, pokud je tlačítko vlastníkem zadaného popisovače okna; v opačném případě 0.

### <a name="remarks"></a>Poznámky

Tato metoda vrátí nenulovou hodnotu, pokud *HWND* buď odkazuje na přímý popisovač okna, nebo je podřízeným prvkem popisovače okna, který je spojen s tlačítkem. Tato metoda vrátí 0, pokud má *HWND* hodnotu null.

##  <a name="isvisible"></a>CMFCToolBarButton:: Visible

Určuje, zda je tlačítko panelu nástrojů viditelné.

```
BOOL IsVisible() const;
```

### <a name="return-value"></a>Návratová hodnota

Nenulové, pokud je tlačítko panelu nástrojů viditelné; v opačném případě 0.

### <a name="remarks"></a>Poznámky

Tlačítko panelu nástrojů můžete zobrazit nebo skrýt pomocí metody [CMFCToolBarButton:: SetVisible](#setvisible) . Po volání [CMFCToolBarButton:: SetVisible](#setvisible) pro přepočítání rozložení nadřazeného panelu nástrojů volejte metodu [CPane:: AdjustSizeImmediate](../../mfc/reference/cpane-class.md#adjustsizeimmediate) na nadřazeném panelu nástrojů.

##  <a name="iswindowvisible"></a>CMFCToolBarButton::IsWindowVisible

Určuje, zda je podkladové okno tlačítka viditelné.

```
virtual BOOL IsWindowVisible();
```

### <a name="return-value"></a>Návratová hodnota

Nenulové, pokud je podkladové okno ovládacího tlačítka viditelné; v opačném případě 0.

### <a name="remarks"></a>Poznámky

Tato metoda vrátí nenulovou hodnotu, pokud atribut Style podkladového popisovače okna obsahuje styl WS_VISIBLE. Tato metoda vrátí hodnotu FALSE, pokud je podkladová obslužná rutina tlačítka NULL.

##  <a name="m_bimage"></a>  CMFCToolBarButton::m_bImage

Určuje, zda je na tlačítku zobrazen obrázek.

```
BOOL m_bImage;
```

### <a name="remarks"></a>Poznámky

Pokud je tento datový člen nastaven na hodnotu TRUE, rozhraní zobrazí obrázek, který je přidružen k tlačítku panelu nástrojů. v opačném případě architektura obrázek nezobrazuje. Tento člen ovlivňuje návratovou hodnotu metody [CMFCToolBarButton:: m_bImage](#m_bimage) .

##  <a name="m_btext"></a>  CMFCToolBarButton::m_bText

Určuje, zda je na tlačítku zobrazen textový popisek.

```
BOOL m_bText;
```

### <a name="remarks"></a>Poznámky

Pokud je tento datový člen nastaven na hodnotu TRUE, rozhraní zobrazí popisek text tlačítka panelu nástrojů. v opačném případě se v rozhraní nezobrazí textový popisek. Tento člen ovlivňuje návratovou hodnotu metody [CMFCToolBarButton:: m_bText](#m_btext) .

##  <a name="m_btextbelow"></a>  CMFCToolBarButton::m_bTextBelow

Určuje, zda je textový popisek zobrazen pod obrázkem na tlačítku.

```
BOOL m_bTextBelow;
```

### <a name="remarks"></a>Poznámky

Pokud je tato proměnná členů nastavena na hodnotu TRUE, rozhraní zobrazí text tlačítka pod obrázkem. Výchozí hodnota tohoto člena je FALSE.

##  <a name="m_buserbutton"></a>CMFCToolBarButton::m_bUserButton

Určuje, jestli má tlačítko uživatelsky definovaný obrázek.

```
BOOL m_bUserButton;
```

### <a name="remarks"></a>Poznámky

Tento datový člen je nastaven na hodnotu TRUE, pokud k tlačítku je přiřazena uživatelem definovaná image.

##  <a name="m_bwholetext"></a>  CMFCToolBarButton::m_bWholeText

Určuje, zda tlačítko zobrazí úplný text popisku, i když se nevejde do ohraničujícího obdélníku.

```
BOOL m_bWholeText;
```

### <a name="remarks"></a>Poznámky

Pokud je tento datový člen nastaven na hodnotu TRUE, rozhraní zobrazí úplný textový popisek zvětšením tlačítka. V opačném případě architektura zkrátí a připojí tři tečky ( **...** ) k textovému popisku.

##  <a name="m_bwrap"></a>  CMFCToolBarButton::m_bWrap

Určuje, zda bude tlačítko vedle oddělovače vloženo na další řádek.

```
BOOL m_bWrap;
```

### <a name="remarks"></a>Poznámky

Rozhraní nastaví tento datový člen na hodnotu TRUE, pokud se tlačítko panelu nástrojů nevejde na aktuální řádek nebo když zadáte rozložení (například určitý počet tlačítek panelu nástrojů na řádek).

Rozhraní umístí toto tlačítko na další řádek, pokud je tento datový člen nastaven na hodnotu TRUE a panel nástrojů je ukotven vodorovně nebo plovoucí.

Výchozí hodnota tohoto datového členu je FALSE.

##  <a name="m_bwraptext"></a>  CMFCToolBarButton::m_bWrapText

Určuje, zda jsou povoleny víceřádkové textové popisky.

```
AFX_IMPORT_DATA static BOOL m_bWrapText;
```

### <a name="remarks"></a>Poznámky

Pokud má tuto statickou členskou proměnnou hodnotu TRUE, rozhraní umožní všem panelům nástrojů Zobrazit víceřádkové textové popisky tlačítek na panelu nástrojů.

Výchozí hodnota tohoto datového členu je FALSE.

##  <a name="m_nid"></a>CMFCToolBarButton::m_nID

ID příkazu tlačítka

```
UINT m_nID;
```

### <a name="remarks"></a>Poznámky

ID příkazu-1 označuje, že tlačítko je oddělovačem. Všechny oddělovače tlačítek mají styl TBBS_SEPARATOR. Další informace o stylech tlačítek naleznete v tématu [CMFCToolBarButton:: m_nStyle](#m_nstyle) .

##  <a name="m_nstyle"></a>CMFCToolBarButton::m_nStyle

Styl tlačítka

```
UINT m_nStyle;
```

### <a name="remarks"></a>Poznámky

Seznam dostupných stylů tlačítek panelu nástrojů najdete v tématu [styly ovládacích prvků panelu nástrojů](../../mfc/reference/toolbar-control-styles.md) .

##  <a name="m_strtext"></a>  CMFCToolBarButton::m_strText

Textový popisek tlačítka

```
CString m_strText;
```

### <a name="remarks"></a>Poznámky

Tento datový člen obsahuje textový popisek tlačítka. Textový popisek může být prázdný.

##  <a name="notifycommand"></a>CMFCToolBarButton::NotifyCommand

Určuje, zda tlačítko zpracovává zprávu [WM_COMMAND](/windows/win32/menurc/wm-command) .

```
virtual BOOL NotifyCommand(int iNotifyCode);
```

### <a name="parameters"></a>Parametry

*iNotifyCode*<br/>
pro Zpráva s oznámením, která je přidružena k příkazu.

### <a name="return-value"></a>Návratová hodnota

Tato metoda vrátí hodnotu FALSE.

### <a name="remarks"></a>Poznámky

Rozhraní volá tuto metodu, když se chystá odeslat zprávu [WM_COMMAND](/windows/win32/menurc/wm-command) do nadřazeného okna.

Ve výchozím nastavení tato metoda vrátí hodnotu FALSE. Přepsáním této metody vrátíte hodnotu TRUE, pokud chcete zpracovat zprávu WM_COMMAND nebo FALSE, aby označovala, že by měl nadřazený panel nástrojů zprávu zpracovat.

##  <a name="onaddtocustomizepage"></a>CMFCToolBarButton::OnAddToCustomizePage

Volá se rozhraním, když se přidá tlačítko do dialogového okna **přizpůsobit** .

```
virtual void OnAddToCustomizePage();
```

### <a name="remarks"></a>Poznámky

Výchozí implementace této metody neprovádí žádnou akci. Tuto metodu přepište, pokud chcete provést určitou akci, když je tlačítko přidáno do dialogového okna **přizpůsobit** .

##  <a name="onbeforedrag"></a>CMFCToolBarButton::OnBeforeDrag

Určuje, zda lze tlačítko přetáhnout.

```
virtual BOOL OnBeforeDrag() const;
```

### <a name="return-value"></a>Návratová hodnota

TRUE, pokud lze tlačítko přetáhnout; v opačném případě FALSE.

### <a name="remarks"></a>Poznámky

Rozhraní volá tuto metodu předtím, než uživatel začne přetáhnout tlačítko.

Výchozí implementace této metody vrátí hodnotu TRUE. Přepsáním této metody vrátíte hodnotu FALSE, čímž zakážete přetahování tlačítka.

##  <a name="onbeforedrop"></a>CMFCToolBarButton::OnBeforeDrop

Určuje, zda uživatel může tlačítko vyřadit na cílový panel nástrojů.

```
virtual BOOL OnBeforeDrop(CMFCToolBar* pTarget);
```

### <a name="parameters"></a>Parametry

*pTarget*<br/>
pro Cíl operace přetažení.

### <a name="return-value"></a>Návratová hodnota

TRUE, pokud se tlačítko dá vyřadit na zadaný panel nástrojů cíl; v opačném případě FALSE.

### <a name="remarks"></a>Poznámky

Rozhraní volá tuto metodu před tím, než je tlačítko přehozeno na panel nástrojů.

Výchozí implementace této metody vrátí hodnotu TRUE. Přepsáním této metody vrátíte hodnotu FALSE, čímž zakážete operaci přetažení v zadaném cíli.

##  <a name="oncalculatesize"></a>CMFCToolBarButton::OnCalculateSize

Volá se rozhraním, aby se počítala velikost tlačítka pro zadaný kontext zařízení a stav docking.

```
virtual SIZE OnCalculateSize(
    CDC* pDC,
    const CSize& sizeDefault,
    BOOL bHorz);
```

### <a name="parameters"></a>Parametry

*pDC*<br/>
pro Kontext zařízení, který zobrazí tlačítko.

*sizeDefault*<br/>
pro Výchozí velikost tlačítka

*bHorz*<br/>
pro Stav Dock ovládacího prvku ToolBar Tento parametr má hodnotu TRUE, pokud je panel nástrojů ukotven vodorovně nebo je plovoucí, nebo FALSE, pokud je panel nástrojů ukotvený svisle.

### <a name="return-value"></a>Návratová hodnota

`SIZE` Struktura obsahující rozměry tlačítka v pixelech

### <a name="remarks"></a>Poznámky

Rozhraní volá tuto metodu, aby určila velikost tlačítka panelu nástrojů pro zadaný kontext zařízení a stav Dock.

Výchozí implementace bere v úvahu velikost textu a obrázků (Pokud se zobrazí), umístění textu a obrázku (text níže nebo na pravé straně obrázku) a stav ukotvení panelu nástrojů.

Tuto metodu přepište, pokud chcete zadat velikost nestandardního tlačítka (například tlačítko pro úpravy pole).

##  <a name="oncancelmode"></a>CMFCToolBarButton::OnCancelMode

Volá se rozhraním, aby se zpracovala zpráva [WM_CANCELMODE](/windows/win32/winmsg/wm-cancelmode) .

```
virtual void OnCancelMode();
```

### <a name="remarks"></a>Poznámky

Výchozí implementace této metody neprovádí žádnou akci. Tuto metodu přepište, pokud chcete zpracovat zprávu [WM_CANCELMODE](/windows/win32/winmsg/wm-cancelmode) .

##  <a name="onchangeparentwnd"></a>CMFCToolBarButton::OnChangeParentWnd

Volá se rozhraním, když se na nový panel nástrojů Vloží tlačítko.

```
virtual void OnChangeParentWnd(CWnd* pWndParent);
```

### <a name="parameters"></a>Parametry

*pWndParent*<br/>
pro Nové nadřazené okno.

### <a name="remarks"></a>Poznámky

Tlačítko je vloženo do panelu nástrojů, například když ho uživatel přetáhne z jednoho panelu nástrojů na jiný panel nástrojů.

Výchozí implementace této metody neprovádí žádnou akci.

##  <a name="onclick"></a>CMFCToolBarButton:: Click

Volá se rozhraním, když uživatel klikne na tlačítko myši.

```
virtual BOOL OnClick(
    CWnd* pWnd,
    BOOL bDelay=TRUE);
```

### <a name="parameters"></a>Parametry

*pWnd*<br/>
pro Nadřazené okno tlačítka panelu nástrojů

*bDelay*<br/>
pro Hodnota TRUE, pokud má být zpráva zpracována zpožděním

### <a name="return-value"></a>Návratová hodnota

Tato metoda vrátí hodnotu FALSE.

### <a name="remarks"></a>Poznámky

Rozhraní volá tuto metodu, když uživatel klikne na tlačítko panelu nástrojů.

Výchozí implementace neprovede žádnou akci a vrátí hodnotu FALSE. Tuto metodu přepište, pokud chcete vrátit nenulovou hodnotu, pokud tlačítko zpracuje zprávu Click.

##  <a name="onclickup"></a>CMFCToolBarButton::OnClickUp

Volá se rozhraním, když uživatel uvolní tlačítko myši.

```
virtual BOOL OnClickUp();
```

### <a name="return-value"></a>Návratová hodnota

Tato metoda vrátí hodnotu FALSE.

### <a name="remarks"></a>Poznámky

Rozhraní volá tuto metodu, když uživatel uvolní tlačítko panelu nástrojů.

Výchozí implementace neprovede žádnou akci a vrátí hodnotu FALSE. Tuto metodu přepište, pokud chcete vrátit nenulovou hodnotu, pokud tlačítko zpracuje zprávu Click.

##  <a name="oncontexthelp"></a>CMFCToolBarButton::OnContextHelp

Volá se rozhraním, když nadřazený panel nástrojů zpracovává zprávu WM_HELPHITTEST.

```
virtual BOOL OnContextHelp(CWnd* pWnd);
```

### <a name="parameters"></a>Parametry

*pWnd*<br/>
pro Nadřazené okno tlačítka panelu nástrojů

### <a name="return-value"></a>Návratová hodnota

Tato metoda vrátí hodnotu FALSE.

### <a name="remarks"></a>Poznámky

Výchozí implementace této metody neprovede žádnou akci a vrátí hodnotu FALSE. Tuto metodu přepište, pokud chcete vrátit nenulovou hodnotu, pokud tlačítko zpracovává zprávu v nápovědě.

Další informace o zprávě WM_HELPHITTEST naleznete v tématu [TN028: Podpora](../../mfc/tn028-context-sensitive-help-support.md)kontextové pomoci.

##  <a name="onctlcolor"></a>CMFCToolBarButton::OnCtlColor

Volá se rozhraním, když nadřazený panel nástrojů zpracovává zprávu WM_CTLCOLOR.

```
virtual HBRUSH OnCtlColor(
    CDC* pDC,
    UINT nCtlColor);
```

### <a name="parameters"></a>Parametry

*pDC*<br/>
pro Kontext zařízení, který zobrazí tlačítko.

*nCtlColor*<br/>
pro Konkrétní barva oznámení.

### <a name="return-value"></a>Návratová hodnota

Popisovač objektu štětce, který používá rozhraní k vykreslování pozadí tlačítka.

### <a name="remarks"></a>Poznámky

Rozhraní volá tuto metodu, když nadřazený panel nástrojů zpracovává zprávu WM_CTLCOLOR pro tlačítko panelu nástrojů, které obsahuje ovládací prvek Windows. Rozhraní nevolá tuto metodu, pokud je tlačítko panelu nástrojů bez okna.

Rozhraní volá tuto metodu, když je rozhraní panelu nástrojů v režimu přizpůsobení a tlačítko panelu nástrojů je odemčeno. Další informace o režimu přizpůsobení naleznete v tématu [CMFCToolBar:: SetCustomizeMode](../../mfc/reference/cmfctoolbar-class.md#setcustomizemode). Další informace o zamykání tlačítek panelu nástrojů naleznete v tématu [CMFCToolBarButton::-Locked](#islocked).

Výchozí implementace neprovede žádnou akci a vrátí hodnotu NULL.

##  <a name="oncustomizemenu"></a>CMFCToolBarButton::OnCustomizeMenu

Umožňuje tlačítku změnit poskytnutou nabídku, když aplikace zobrazuje místní nabídku na nadřazeném panelu nástrojů.

```
virtual BOOL OnCustomizeMenu(CMenu* pMenu);
```

### <a name="parameters"></a>Parametry

*pMenu*<br/>
pro Nabídka, která se má přizpůsobit

### <a name="return-value"></a>Návratová hodnota

Tato metoda vrátí hodnotu FALSE.

### <a name="remarks"></a>Poznámky

Výchozí implementace neprovede žádnou akci a vrátí hodnotu FALSE. Potlačí tuto metodu a vrátí nenulovou hodnotu, pokud chcete upravit obsah poskytnuté nabídky.

##  <a name="ondblclk"></a>  CMFCToolBarButton::OnDblClk

Volá se rozhraním, když nadřazený panel nástrojů zpracovává zprávu [WM_LBUTTONDBLCLK](/windows/win32/inputdev/wm-lbuttondblclk) .

```
virtual void OnDblClk(CWnd* pWnd);
```

### <a name="parameters"></a>Parametry

*pWnd*<br/>
[in] – nadřazené okno tlačítka

### <a name="remarks"></a>Poznámky

Tato metoda je volána `CMFCToolBar::OnLButtonDblClk` metodou, když nadřazený panel nástrojů zpracuje zprávu [WM_LBUTTONDBLCLK](/windows/win32/inputdev/wm-lbuttondblclk) .

Výchozí implementace této metody neprovádí žádnou akci.

##  <a name="ondraw"></a>CMFCToolBarButton:: Draw

Volá se rozhraním, aby se vykreslilo tlačítko pomocí zadaných stylů a možností.

```
virtual void OnDraw(
    CDC* pDC,
    const CRect& rect,
    CMFCToolBarImages* pImages,
    BOOL bHorz=TRUE,
    BOOL bCustomizeMode=FALSE,
    BOOL bHighlight=FALSE,
    BOOL bDrawBorder=TRUE,
    BOOL bGrayDisabledButtons=TRUE);
```

### <a name="parameters"></a>Parametry

*pDC*<br/>
pro Kontext zařízení, který zobrazí tlačítko.

*OBD*<br/>
pro Ohraničující obdélník tlačítka

*pImages*<br/>
pro Kolekce obrázků panelů nástrojů, které jsou přidruženy k tlačítku.

*bHorz*<br/>
pro Stav Dock ovládacího prvku ToolBar Tento parametr má hodnotu TRUE, pokud je tlačítko ukotveno vodorovně a hodnotu FALSE, pokud je tlačítko ukotveno svisle.

*bCustomizeMode*<br/>
pro Určuje, zda je panel nástrojů v režimu přizpůsobení. Tento parametr má hodnotu TRUE, pokud je panel nástrojů v režimu přizpůsobení a hodnota FALSE v případě, že panel nástrojů není v režimu přizpůsobení.

*bHighlight*<br/>
pro Určuje, zda je tlačítko zvýrazněno. Tento parametr má hodnotu TRUE, pokud je tlačítko zvýrazněno a FALSE, pokud tlačítko není zvýrazněno.

*bDrawBorder*<br/>
pro Určuje, zda má tlačítko zobrazit jeho ohraničení. Tento parametr má hodnotu TRUE, pokud má tlačítko zobrazit jeho ohraničení a hodnotu FALSE, pokud by tlačítko neměl zobrazit jeho ohraničení.

*bGrayDisabledButtons*<br/>
pro Určuje, zda se mají zakázaná tlačítka odstínovat nebo použít kolekci zakázaných imagí. Tento parametr má hodnotu TRUE, pokud mají být zakázaná tlačítka šedá a FALSE, pokud by tato metoda měla používat kolekci zakázaných imagí.

### <a name="remarks"></a>Poznámky

Tuto metodu přepište, pokud chcete přizpůsobit vykreslování tlačítek panelu nástrojů.

##  <a name="ondrawoncustomizelist"></a>CMFCToolBarButton::OnDrawOnCustomizeList

Volá se rozhraním, aby se nakreslilo tlačítko v podokně **příkazy** dialogového okna **přizpůsobit** .

```
virtual int OnDrawOnCustomizeList(
    CDC* pDC,
    const CRect& rect,
    BOOL bSelected);
```

### <a name="parameters"></a>Parametry

*pDC*<br/>
pro Kontext zařízení, který zobrazí tlačítko.

*OBD*<br/>
pro Ohraničující obdélník tlačítka

*bSelected*<br/>
pro Určuje, zda je vybráno tlačítko. Je-li tento parametr nastaven na hodnotu TRUE, je tlačítko vybráno. Pokud je tento parametr FALSE, tlačítko není vybráno.

### <a name="return-value"></a>Návratová hodnota

Šířka tlačítka na zadaném kontextu zařízení (v pixelech).

### <a name="remarks"></a>Poznámky

Tato metoda je volána dialogovým oknem přizpůsobení (karta **příkazy** ), když se tlačítko zobrazí v seznamu vykresleném vlastníkem.

Výchozí implementace této metody zobrazí obrázek a textový popisek tlačítka, pokud jsou k dispozici. Pokud není textový popisek tlačítka k dispozici, metoda zobrazí text popisku.

Tuto metodu přepište, pokud chcete provádět vlastní vykreslování.

##  <a name="ongetcustomtooltiptext"></a>CMFCToolBarButton::OnGetCustomToolTipText

Volá se rozhraním, aby se získal text vlastního popisu tlačítka.

```
virtual BOOL OnGetCustomToolTipText(CString& strToolTip);
```

### <a name="parameters"></a>Parametry

*strToolTip*<br/>
mimo `CString` Objekt, který obdrží text vlastního popisu tlačítka.

### <a name="return-value"></a>Návratová hodnota

Tato metoda vrátí hodnotu FALSE.

### <a name="remarks"></a>Poznámky

Rozhraní volá tuto metodu, když zobrazí popis tlačítka panelu nástrojů. Pokud tato metoda vrátí hodnotu FALSE, použije rozhraní výchozí popis.

Výchozí implementace neprovede žádnou akci a vrátí hodnotu FALSE. Potlačí tuto metodu a vrátí nenulovou hodnotu pro zadání vlastního textu popisku pro tlačítko panelu nástrojů.

##  <a name="onglobalfontschanged"></a>  CMFCToolBarButton::OnGlobalFontsChanged

Volá se rozhraním, když se změní globální písmo.

```
virtual void OnGlobalFontsChanged();
```

### <a name="remarks"></a>Poznámky

Výchozí implementace této metody neprovádí žádnou akci. Tuto metodu přepište, pokud chcete aktualizovat písmo, které se používá k zobrazení textu tlačítka.

##  <a name="onmove"></a>CMFCToolBarButton::-Move

Volá se rozhraním, když se přesune nadřazený panel nástrojů.

```
virtual void OnMove();
```

### <a name="remarks"></a>Poznámky

Výchozí implementace této metody neprovádí žádnou akci. Tuto metodu přepište, chcete-li změnit umístění tlačítka při přesunu nadřazeného panelu nástrojů.

##  <a name="onshow"></a>CMFCToolBarButton:: inshow

Volá se rozhraním, když se tlačítko zobrazí jako viditelné nebo neviditelné.

```
virtual void OnShow(BOOL bShow);
```

### <a name="parameters"></a>Parametry

*bShow*<br/>
pro Určuje, zda je tlačítko viditelné. Pokud má tento parametr hodnotu TRUE, tlačítko je viditelné. V opačném případě není tlačítko viditelné.

### <a name="remarks"></a>Poznámky

Výchozí implementace této metody neprovádí žádnou akci. Tuto metodu přepište, chcete-li aktualizovat viditelnost tlačítka.

##  <a name="onsize"></a>CMFCToolBarButton::-size

Volá se rozhraním, když se změní jeho velikost nebo pozice na nadřazeném panelu nástrojů a tato změna způsobí změnu velikosti tlačítka.

```
virtual void OnSize(int iSize);
```

### <a name="parameters"></a>Parametry

*iSize*<br/>
pro Nová šířka tlačítka

### <a name="remarks"></a>Poznámky

Výchozí implementace této metody neprovádí žádnou akci. Přepište tuto metodu pro změnu velikosti tlačítka, když se změní velikost nebo poloha nadřazeného panelu nástrojů.

##  <a name="ontoolhittest"></a>CMFCToolBarButton::OnToolHitTest

Volá se rozhraním, když nadřazený panel nástrojů musí určit, jestli je bod v ohraničujícím obdélníku tlačítka.

```
virtual BOOL OnToolHitTest(
    const CWnd* pWnd,
    TOOLINFO* pTI);
```

### <a name="parameters"></a>Parametry

*pWnd*<br/>
pro Nadřazené okno tlačítka Může mít hodnotu NULL.

*pTI*<br/>
pro `TOOLINFO` Struktura, která obsahuje informace o nástroji v ovládacím prvku ToolTip.

### <a name="return-value"></a>Návratová hodnota

Výsledek, `OnMenuButtonToolHitTest` Pokud tlačítko může načíst ukazatel do okna nadřazeného rámce; v opačném případě false.

### <a name="remarks"></a>Poznámky

Tato metoda volá jednu z následujících metod, pokud může převést nadřazené okno na platný objekt Frame:

- [CMDIFrameWndEx::OnMenuButtonToolHitTest](../../mfc/reference/cmdiframewndex-class.md#onmenubuttontoolhittest)

- [CFrameWndEx::OnMenuButtonToolHitTest](../../mfc/reference/cframewndex-class.md#onmenubuttontoolhittest)

- [COleIPFrameWndEx::OnMenuButtonToolHitTest](../../mfc/reference/coleipframewndex-class.md#onmenubuttontoolhittest)

##  <a name="onupdatetooltip"></a>CMFCToolBarButton::OnUpdateToolTip

Volá se rozhraním, když nadřazený panel nástrojů aktualizuje svůj text popisku.

```
virtual BOOL OnUpdateToolTip(
    CWnd* pWndParent,
    int iButtonIndex,
    CToolTipCtrl& wndToolTip,
    CString& str);
```

### <a name="parameters"></a>Parametry

*pWndParent*<br/>
pro Nadřazené okno

*iButtonIndex*<br/>
pro Index tlačítka založený na nule v nadřazené kolekci tlačítek.

*wndToolTip*<br/>
pro Ovládací prvek, který zobrazuje text popisku

*str*<br/>
mimo `CString` Objekt, který obdrží aktualizovaný text popisku.

### <a name="return-value"></a>Návratová hodnota

Tato metoda vrátí hodnotu FALSE.

### <a name="remarks"></a>Poznámky

Výchozí implementace této metody neprovede žádnou akci a vrátí hodnotu FALSE. Přepsat tuto metodu, pokud zadáte textový řetězec popisku, vrátí nenulovou hodnotu.

##  <a name="preparedrag"></a>CMFCToolBarButton::P repareDrag

Volá se rozhraním, když má tlačítko k provedení operace přetažení.

```
virtual BOOL PrepareDrag(COleDataSource& srcItem);
```

### <a name="parameters"></a>Parametry

*srcItem*<br/>
pro `COleDataSource` Objekt, který ukládá informace o stavu operace přetažení.

### <a name="return-value"></a>Návratová hodnota

TRUE, pokud je operace úspěšná; v opačném případě FALSE.

### <a name="remarks"></a>Poznámky

Rozhraní volá tuto metodu pro přípravu tlačítka panelu nástrojů pro uložení stavu v poskytnutém `COleDataSource` objektu. Tato metoda ukládá svůj stav pomocí serializace sebe sama do sdíleného souboru a poté předává tento soubor do metody [COleDataSource –:: CacheGlobalData](../../mfc/reference/coledatasource-class.md#cacheglobaldata) . Další informace o serializaci tlačítek na panelu nástrojů naleznete v tématu [CMFCToolBarButton:: serializovat](#serialize).

Tato metoda neprovede žádnou akci a vrátí hodnotu TRUE, pokud tlačítko nelze uložit (metoda [CMFCToolBarButton:: CanBeStored](#canbestored) vrátí hodnotu false). Vrátí hodnotu FALSE, pokud dojde k výjimce při serializaci objektu.

Další informace o operacích přetahování OLE naleznete v tématu přetažení [(OLE)](../../mfc/drag-and-drop-ole.md).

##  <a name="rect"></a>CMFCToolBarButton:: Rect

Načte ohraničující obdélník tlačítka.

```
const CRect& Rect() const;
```

### <a name="return-value"></a>Návratová hodnota

`CRect` Objekt, který obsahuje ohraničující obdélník tlačítka.

##  <a name="resetimagetodefault"></a>CMFCToolBarButton::ResetImageToDefault

Nastaví výchozí hodnotu na obrázek, který je přidružený k tlačítku.

```
virtual void ResetImageToDefault();
```

### <a name="remarks"></a>Poznámky

Tato metoda načte výchozí obrázek ze svého nadřazeného panelu nástrojů pomocí metody [CMFCToolBar:: GetDefaultImage](../../mfc/reference/cmfctoolbar-class.md#getdefaultimage) . Pokud tlačítko nemá přidružený výchozí obrázek, tato metoda nastaví textový popisek tlačítka podle jeho řetězcového prostředku pomocí metody [CStringT:: LoadString](../../atl-mfc-shared/reference/cstringt-class.md#loadstring) . Další informace o zdrojích řetězců naleznete v tématu [práce se soubory prostředků](../../windows/working-with-resource-files.md).

Tato metoda neprovede žádnou akci, pokud má tlačítko uživatelem definovaný obrázek.

##  <a name="savebarstate"></a>CMFCToolBarButton::SaveBarState

Uloží stav tlačítka panelu nástrojů.

```
virtual void SaveBarState();
```

### <a name="remarks"></a>Poznámky

Rozhraní volá tuto metodu, když vytvoří `CMFCToolBarButton` objekt jako výsledek operace přetažení.

Výchozí implementace této metody neprovádí žádnou akci. Tuto metodu přepište, pokud chcete uložit stav tlačítka panelu nástrojů na externí zdroj dat.

##  <a name="serialize"></a>CMFCToolBarButton:: serializovat

Přečte tento objekt z archivu nebo ho zapíše do archivu.

```
virtual void Serialize(CArchive& ar);
```

### <a name="parameters"></a>Parametry

*snížen*<br/>
pro `CArchive` Objekt, ze kterého se má serializovat.

### <a name="remarks"></a>Poznámky

Tato metoda podporuje procesy přenosu dat, jako je například schránka nebo operace přetažení. Čte nebo zapisuje vlastnosti tlačítka, jako je ID, textový popisek a ID obrázku z nebo na poskytnutý `CArchive` objekt.

Příklady serializace naleznete v [tématu serializace: Serializace objektu](../../mfc/serialization-serializing-an-object.md).

##  <a name="setaccdata"></a>CMFCToolBarButton::SetACCData

Naplní poskytnutý `CAccessibilityData` objekt daty přístupnosti z tlačítka panelu nástrojů.

```
virtual BOOL SetACCData(
    CWnd* pParent,
    CAccessibilityData& data);
```

### <a name="parameters"></a>Parametry

*pParent*<br/>
pro Nadřazené okno tlačítka panelu nástrojů

*data*<br/>
pro `CAccessibilityData` Objekt, který je vyplněn daty přístupnosti tlačítka panelu nástrojů.

### <a name="return-value"></a>Návratová hodnota

Tato metoda vrátí hodnotu TRUE.

### <a name="remarks"></a>Poznámky

Tuto metodu přepište, pokud tlačítko na panelu nástrojů neposkytuje data přístupnosti, aby vracela hodnotu FALSE.

##  <a name="setclipboardformatname"></a>CMFCToolBarButton::SetClipboardFormatName

Přejmenuje globální formát schránky.

```
static void __stdcall SetClipboardFormatName(LPCTSTR lpszName);
```

### <a name="parameters"></a>Parametry

*lpszName*<br/>
pro Nový název globálního formátu schránky. Nemůže mít hodnotu NULL.

### <a name="remarks"></a>Poznámky

Tato metoda umožňuje provádět operace přetažení mezi více aplikacemi. Každá aplikace musí poskytovat stejný název formátu schránky.

Tuto metodu je nutné volat před voláním [CMFCToolBarButton:: GetClipboardFormat](#getclipboardformat).

##  <a name="setimage"></a>CMFCToolBarButton::SetImage

Nastaví index obrázku tlačítka.

```
virtual void SetImage(int iImage);
```

### <a name="parameters"></a>Parametry

*iImage*<br/>
pro Index obrázku v kolekci obrázků panelu nástrojů

### <a name="remarks"></a>Poznámky

Pokud je tlačítko panelu nástrojů oddělovač, *iImage* odkazuje na novou šířku tlačítka oddělovače.

Pokud je *iImage* menší než nula, tato metoda zakáže vykreslování obrázku a povolí kreslení popisku textu tlačítka.

##  <a name="setprotectedcommands"></a>CMFCToolBarButton::SetProtectedCommands

Nastaví seznam příkazů, které uživatel nemůže přizpůsobit.

```
static void SetProtectedCommands(const CList<UINT,UINT>& lstCmds);
```

### <a name="parameters"></a>Parametry

*lstCmds*<br/>
pro Seznam chráněných příkazů.

### <a name="remarks"></a>Poznámky

V režimu přizpůsobení rozhraní zakáže příkazy tlačítek na panelu nástrojů, které jsou chráněné. Uživatel nemůže provádět operace přetahování a úprav na zakázaných tlačítkách na panelu nástrojů.

K načtení seznamu chráněných příkazů použijte metodu [CMFCToolBarButton:: GetProtectedCommands](#getprotectedcommands) .

##  <a name="setradio"></a>CMFCToolBarButton::SetRadio

Volá se rozhraním, když se změní stav zaškrtnutí tlačítka.

```
virtual void SetRadio();
```

### <a name="remarks"></a>Poznámky

Výchozí implementace této metody neprovádí žádnou akci. Přepište tuto metodu pro provedení vlastní akce, když tlačítko změní stav zaškrtnutí.

##  <a name="setrect"></a>CMFCToolBarButton::SetRect

Nastaví ohraničující obdélník tlačítka.

```
void SetRect(const CRect rect);
```

### <a name="parameters"></a>Parametry

*OBD*<br/>
pro Nový ohraničující obdélník tlačítka.

### <a name="remarks"></a>Poznámky

Tato metoda volá metodu [CMFCToolBarButton::-Move](#onmove) po nastavení nového ohraničujícího obdélníku.

##  <a name="setstyle"></a>CMFCToolBarButton:: SetStyle

Nastaví styl tlačítka.

```
virtual void SetStyle(UINT nStyle);
```

### <a name="parameters"></a>Parametry

*nStyle*<br/>
pro Nový styl tlačítka

### <a name="remarks"></a>Poznámky

Výchozí implementace nastaví datový člen [CMFCToolBarButton:: m_nStyle](#m_nstyle) na *nStyle*. Tuto metodu přepište, pokud chcete provést další zpracování pro zpracování změny ve stylu. Seznam platných příznaků stylu naleznete v tématu [styly ovládacích prvků panelu nástrojů](toolbar-control-styles.md) .

##  <a name="setvisible"></a>CMFCToolBarButton:: SetVisible

Určuje, zda je tlačítko viditelné.

```
void SetVisible(BOOL bShow=TRUE);
```

### <a name="parameters"></a>Parametry

*bShow*<br/>
pro Logická hodnota, která určuje, zda se má tlačítko Zobrazit nebo skrýt. Pokud má tento parametr hodnotu TRUE, zobrazí se tlačítko. Pokud má parametr hodnotu FALSE, tlačítko je skryté.

### <a name="remarks"></a>Poznámky

Pomocí této funkce lze skrýt nebo zobrazit konkrétní tlačítko panelu nástrojů. Po volání této metody zavolejte metodu [CPane:: AdjustSizeImmediate](../../mfc/reference/cpane-class.md#adjustsizeimmediate) .

##  <a name="show"></a>CMFCToolBarButton:: show

Zobrazí nebo skryje tlačítko.

```
void Show(BOOL bShow);
```

### <a name="parameters"></a>Parametry

*bShow*<br/>
pro Logická hodnota, která určuje, zda se má tlačítko Zobrazit nebo skrýt. Pokud má tento parametr hodnotu TRUE, zobrazí se tlačítko. Pokud má parametr hodnotu FALSE, tlačítko je skryté.

### <a name="remarks"></a>Poznámky

Rozhraní volá tuto metodu, aby aktualizovala viditelnost tlačítek panelu nástrojů při změně velikosti jejich nadřazeného panelu nástrojů. Rozhraní volá tuto metodu s *bShow* nastavenou na hodnotu false, pokud se tlačítko již nevejde do hranice panelu nástrojů. Rozhraní volá tuto metodu s *bShow* nastavenou na hodnotu true, když se znovu přizpůsobí velikost tlačítka v mezích panelu nástrojů.

K nastavení Obecné viditelnosti tlačítka použijte metodu [CMFCToolBarButton:: SetVisible](#setvisible) .

Tato metoda volá metodu [CMFCToolBarButton:: inshow](#onshow) poté, co aktualizuje stav viditelnosti tlačítka.

## <a name="see-also"></a>Viz také:

[Graf hierarchie](../../mfc/hierarchy-chart.md)<br/>
[Třídy](../../mfc/reference/mfc-classes.md)<br/>
[CMFCToolBar – třída](../../mfc/reference/cmfctoolbar-class.md)<br/>
[CMFCToolBarImages – třída](../../mfc/reference/cmfctoolbarimages-class.md)
