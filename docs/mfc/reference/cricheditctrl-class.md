---
title: CRichEditCtrl – třída
ms.date: 11/04/2016
f1_keywords:
- CRichEditCtrl
- AFXCMN/CRichEditCtrl
- AFXCMN/CRichEditCtrl::CRichEditCtrl
- AFXCMN/CRichEditCtrl::CanPaste
- AFXCMN/CRichEditCtrl::CanRedo
- AFXCMN/CRichEditCtrl::CanUndo
- AFXCMN/CRichEditCtrl::CharFromPos
- AFXCMN/CRichEditCtrl::Clear
- AFXCMN/CRichEditCtrl::Copy
- AFXCMN/CRichEditCtrl::Create
- AFXCMN/CRichEditCtrl::CreateEx
- AFXCMN/CRichEditCtrl::Cut
- AFXCMN/CRichEditCtrl::DisplayBand
- AFXCMN/CRichEditCtrl::EmptyUndoBuffer
- AFXCMN/CRichEditCtrl::FindText
- AFXCMN/CRichEditCtrl::FindWordBreak
- AFXCMN/CRichEditCtrl::FormatRange
- AFXCMN/CRichEditCtrl::GetCharPos
- AFXCMN/CRichEditCtrl::GetDefaultCharFormat
- AFXCMN/CRichEditCtrl::GetEventMask
- AFXCMN/CRichEditCtrl::GetFirstVisibleLine
- AFXCMN/CRichEditCtrl::GetIRichEditOle
- AFXCMN/CRichEditCtrl::GetLimitText
- AFXCMN/CRichEditCtrl::GetLine
- AFXCMN/CRichEditCtrl::GetLineCount
- AFXCMN/CRichEditCtrl::GetModify
- AFXCMN/CRichEditCtrl::GetOptions
- AFXCMN/CRichEditCtrl::GetParaFormat
- AFXCMN/CRichEditCtrl::GetPunctuation
- AFXCMN/CRichEditCtrl::GetRect
- AFXCMN/CRichEditCtrl::GetRedoName
- AFXCMN/CRichEditCtrl::GetSel
- AFXCMN/CRichEditCtrl::GetSelectionCharFormat
- AFXCMN/CRichEditCtrl::GetSelectionType
- AFXCMN/CRichEditCtrl::GetSelText
- AFXCMN/CRichEditCtrl::GetTextLength
- AFXCMN/CRichEditCtrl::GetTextLengthEx
- AFXCMN/CRichEditCtrl::GetTextMode
- AFXCMN/CRichEditCtrl::GetTextRange
- AFXCMN/CRichEditCtrl::GetUndoName
- AFXCMN/CRichEditCtrl::GetWordWrapMode
- AFXCMN/CRichEditCtrl::HideSelection
- AFXCMN/CRichEditCtrl::LimitText
- AFXCMN/CRichEditCtrl::LineFromChar
- AFXCMN/CRichEditCtrl::LineIndex
- AFXCMN/CRichEditCtrl::LineLength
- AFXCMN/CRichEditCtrl::LineScroll
- AFXCMN/CRichEditCtrl::Paste
- AFXCMN/CRichEditCtrl::PasteSpecial
- AFXCMN/CRichEditCtrl::PosFromChar
- AFXCMN/CRichEditCtrl::Redo
- AFXCMN/CRichEditCtrl::ReplaceSel
- AFXCMN/CRichEditCtrl::RequestResize
- AFXCMN/CRichEditCtrl::SetAutoURLDetect
- AFXCMN/CRichEditCtrl::SetBackgroundColor
- AFXCMN/CRichEditCtrl::SetDefaultCharFormat
- AFXCMN/CRichEditCtrl::SetEventMask
- AFXCMN/CRichEditCtrl::SetModify
- AFXCMN/CRichEditCtrl::SetOLECallback
- AFXCMN/CRichEditCtrl::SetOptions
- AFXCMN/CRichEditCtrl::SetParaFormat
- AFXCMN/CRichEditCtrl::SetPunctuation
- AFXCMN/CRichEditCtrl::SetReadOnly
- AFXCMN/CRichEditCtrl::SetRect
- AFXCMN/CRichEditCtrl::SetSel
- AFXCMN/CRichEditCtrl::SetSelectionCharFormat
- AFXCMN/CRichEditCtrl::SetTargetDevice
- AFXCMN/CRichEditCtrl::SetTextMode
- AFXCMN/CRichEditCtrl::SetUndoLimit
- AFXCMN/CRichEditCtrl::SetWordCharFormat
- AFXCMN/CRichEditCtrl::SetWordWrapMode
- AFXCMN/CRichEditCtrl::StopGroupTyping
- AFXCMN/CRichEditCtrl::StreamIn
- AFXCMN/CRichEditCtrl::StreamOut
- AFXCMN/CRichEditCtrl::Undo
helpviewer_keywords:
- CRichEditCtrl [MFC], CRichEditCtrl
- CRichEditCtrl [MFC], CanPaste
- CRichEditCtrl [MFC], CanRedo
- CRichEditCtrl [MFC], CanUndo
- CRichEditCtrl [MFC], CharFromPos
- CRichEditCtrl [MFC], Clear
- CRichEditCtrl [MFC], Copy
- CRichEditCtrl [MFC], Create
- CRichEditCtrl [MFC], CreateEx
- CRichEditCtrl [MFC], Cut
- CRichEditCtrl [MFC], DisplayBand
- CRichEditCtrl [MFC], EmptyUndoBuffer
- CRichEditCtrl [MFC], FindText
- CRichEditCtrl [MFC], FindWordBreak
- CRichEditCtrl [MFC], FormatRange
- CRichEditCtrl [MFC], GetCharPos
- CRichEditCtrl [MFC], GetDefaultCharFormat
- CRichEditCtrl [MFC], GetEventMask
- CRichEditCtrl [MFC], GetFirstVisibleLine
- CRichEditCtrl [MFC], GetIRichEditOle
- CRichEditCtrl [MFC], GetLimitText
- CRichEditCtrl [MFC], GetLine
- CRichEditCtrl [MFC], GetLineCount
- CRichEditCtrl [MFC], GetModify
- CRichEditCtrl [MFC], GetOptions
- CRichEditCtrl [MFC], GetParaFormat
- CRichEditCtrl [MFC], GetPunctuation
- CRichEditCtrl [MFC], GetRect
- CRichEditCtrl [MFC], GetRedoName
- CRichEditCtrl [MFC], GetSel
- CRichEditCtrl [MFC], GetSelectionCharFormat
- CRichEditCtrl [MFC], GetSelectionType
- CRichEditCtrl [MFC], GetSelText
- CRichEditCtrl [MFC], GetTextLength
- CRichEditCtrl [MFC], GetTextLengthEx
- CRichEditCtrl [MFC], GetTextMode
- CRichEditCtrl [MFC], GetTextRange
- CRichEditCtrl [MFC], GetUndoName
- CRichEditCtrl [MFC], GetWordWrapMode
- CRichEditCtrl [MFC], HideSelection
- CRichEditCtrl [MFC], LimitText
- CRichEditCtrl [MFC], LineFromChar
- CRichEditCtrl [MFC], LineIndex
- CRichEditCtrl [MFC], LineLength
- CRichEditCtrl [MFC], LineScroll
- CRichEditCtrl [MFC], Paste
- CRichEditCtrl [MFC], PasteSpecial
- CRichEditCtrl [MFC], PosFromChar
- CRichEditCtrl [MFC], Redo
- CRichEditCtrl [MFC], ReplaceSel
- CRichEditCtrl [MFC], RequestResize
- CRichEditCtrl [MFC], SetAutoURLDetect
- CRichEditCtrl [MFC], SetBackgroundColor
- CRichEditCtrl [MFC], SetDefaultCharFormat
- CRichEditCtrl [MFC], SetEventMask
- CRichEditCtrl [MFC], SetModify
- CRichEditCtrl [MFC], SetOLECallback
- CRichEditCtrl [MFC], SetOptions
- CRichEditCtrl [MFC], SetParaFormat
- CRichEditCtrl [MFC], SetPunctuation
- CRichEditCtrl [MFC], SetReadOnly
- CRichEditCtrl [MFC], SetRect
- CRichEditCtrl [MFC], SetSel
- CRichEditCtrl [MFC], SetSelectionCharFormat
- CRichEditCtrl [MFC], SetTargetDevice
- CRichEditCtrl [MFC], SetTextMode
- CRichEditCtrl [MFC], SetUndoLimit
- CRichEditCtrl [MFC], SetWordCharFormat
- CRichEditCtrl [MFC], SetWordWrapMode
- CRichEditCtrl [MFC], StopGroupTyping
- CRichEditCtrl [MFC], StreamIn
- CRichEditCtrl [MFC], StreamOut
- CRichEditCtrl [MFC], Undo
ms.assetid: 2be52788-822c-4c27-aafd-2471231e74eb
ms.openlocfilehash: 1c601d036d05048bff9b60f837140e8f7a873964
ms.sourcegitcommit: 7a6116e48c3c11b97371b8ae4ecc23adce1f092d
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/22/2020
ms.locfileid: "81754428"
---
# <a name="cricheditctrl-class"></a>CRichEditCtrl – třída

Poskytuje funkce ovládacího prvku rich edit.

## <a name="syntax"></a>Syntaxe

```
class CRichEditCtrl : public CWnd
```

## <a name="members"></a>Členové

### <a name="public-constructors"></a>Veřejné konstruktory

|Název|Popis|
|----------|-----------------|
|[CRichEditCtrl::CRichEditCtrl](#cricheditctrl)|Vytvoří `CRichEditCtrl` objekt.|

### <a name="public-methods"></a>Veřejné metody

|Název|Popis|
|----------|-----------------|
|[CricheditCtrl::CanPaste](#canpaste)|Určuje, zda lze obsah schránky vložit do tohoto rozšířeného ovládacího prvku úprav.|
|[CRichEditCtrl::CanRedo](#canredo)|Určuje, zda jsou ve frontě opakování ovládacího prvku nějaké akce.|
|[CRichEditCtrl::CanUndo](#canundo)|Určuje, zda lze operaci úprav vrátit zpět.|
|[CRichEditCtrl::CharFromPos](#charfrompos)|Načte informace o znaku, který je nejblíže určenému bodu v klientské oblasti ovládacího prvku pro úpravy.|
|[CRichEditCtrl::Vymazat](#clear)|Vymaže aktuální výběr.|
|[CRichEditCtrl::Kopírovat](#copy)|Zkopíruje aktuální výběr do schránky.|
|[CRichEditCtrl::Vytvořit](#create)|Vytvoří ovládací prvek windows rich edit `CRichEditCtrl` a přidruží jej k tomuto objektu.|
|[CRichEditCtrl::CreateEx](#createex)|Vytvoří ovládací prvek s bohatými úpravami systému `CRichEditCtrl` Windows se zadanými rozšířenými styly systému Windows a přidruží jej k tomuto objektu.|
|[CRichEditCtrl::Vyjmout](#cut)|Vyjme aktuální výběr do schránky.|
|[CRichEditCtrl::DisplayBand](#displayband)|Zobrazí část obsahu tohoto `CRichEditCtrl` objektu.|
|[CRichEditCtrl::EmptyUndoBuffer](#emptyundobuffer)|Obnoví (vymaže) příznak `CRichEditCtrl` vrátit tento objekt.|
|[CRichEditCtrl::FindText](#findtext)|Vyhledá text v `CRichEditCtrl` tomto objektu.|
|[CRichEditCtrl::FindWordBreak](#findwordbreak)|Najde další konec slova před nebo za zadanou pozici znaku nebo načte informace o znaku na této pozici.|
|[CRichEditCtrl::FormatRange](#formatrange)|Formátuje rozsah textu pro cílové výstupní zařízení.|
|[CRichEditCtrl::GetCharPos](#getcharpos)|Určuje umístění daného znaku v `CRichEditCtrl` rámci tohoto objektu.|
|[CRichEditCtrl::GetDefaultCharFormat](#getdefaultcharformat)|Načte aktuální výchozí atributy formátování znaků `CRichEditCtrl` v tomto objektu.|
|[CRichEditCtrl::GetEventMask](#geteventmask)|Načte masku události `CRichEditCtrl` pro tento objekt.|
|[CRichEditCtrl::GetFirstVisibleLine](#getfirstvisibleline)|Určuje nejvyšší viditelnou `CRichEditCtrl` čáru v tomto objektu.|
|[Cricheditctrl::getiricheditole](#getiricheditole)|Načte ukazatel na `IRichEditOle` rozhraní pro tento ovládací prvek rich edit.|
|[CRichEditCtrl::GetLimitText](#getlimittext)|Získá limit na množství textu, který může `CRichEditCtrl` uživatel zadat do tohoto objektu.|
|[CRichEditCtrl::GetLine](#getline)|Načte řádek textu z `CRichEditCtrl` tohoto objektu.|
|[CRichEditCtrl::GetLineCount](#getlinecount)|Načte počet řádků v `CRichEditCtrl` tomto objektu.|
|[CRichEditCtrl::GetModify](#getmodify)|Určuje, zda se `CRichEditCtrl` obsah tohoto objektu od posledního uložení změnil.|
|[CRichEditCtrl::GetOptions](#getoptions)|Načte možnosti rozšířeného ovládacího prvku úprav.|
|[CRichEditCtrl::GetParaFormat](#getparaformat)|Načte atributy formátování odstavce v aktuálním `CRichEditCtrl` výběru v tomto objektu.|
|[CRichEditCtrl::GetPunctuation](#getpunctuation)|Načte aktuální interpunkční znaky pro ovládací prvek rich edit. Tato zpráva je k dispozici pouze v asijských jazykových verzích operačního systému.|
|[CRichEditCtrl::GetRect](#getrect)|Načte formátovací obdélník pro `CRichEditCtrl` tento objekt.|
|[CRichEditCtrl::GetRedoName](#getredoname)|Načte typ další akce, pokud existuje, ve frontě opakování ovládacího prvku.|
|[CRichEditCtrl::GetSel](#getsel)|Získá počáteční a koncové pozice aktuálního `CRichEditCtrl` výběru v tomto objektu.|
|[CRichEditCtrl::GetSelectionCharFormat](#getselectioncharformat)|Načte atributy formátování znaků v aktuálním `CRichEditCtrl` výběru v tomto objektu.|
|[CRichEditCtrl::GetSelectionType](#getselectiontype)|Načte typ obsahu v aktuálním `CRichEditCtrl` výběru v tomto objektu.|
|[CRichEditCtrl::GetSelText](#getseltext)|Získá text aktuálního výběru `CRichEditCtrl` v tomto objektu|
|[CRichEditCtrl::GetTextLength](#gettextlength)|Načte délku textu ve znacích `CRichEditCtrl` v tomto objektu. Neobsahuje ukončující znak null.|
|[CRichEditCtrl::GetTextLengthEx](#gettextlengthex)|Načte počet znaků nebo bajtů v rozšířeném zobrazení úprav. Přijme seznam příznaků označujících metodu určení délky textu v ovládacím prvku rich edit.|
|[CRichEditCtrl::GetTextMode](#gettextmode)|Načte aktuální textový režim a úroveň rozšířené úpravy.|
|[CRichEditCtrl::GetTextRange](#gettextrange)|Načte zadaný rozsah textu.|
|[CRichEditCtrl::GetUndoName](#getundoname)|Načte typ další akce zpět, pokud existuje.|
|[CRichEditCtrl::GetWordWrapMode](#getwordwrapmode)|Načte aktuální možnosti zalamování slov a rozdělení slov pro ovládací prvek rich edit. Tato zpráva je k dispozici pouze v asijských jazykových verzích operačního systému.|
|[CRichEditCtrl::HideSelection](#hideselection)|Zobrazí nebo skryje aktuální výběr.|
|[cRichEditCtrl::LimitText](#limittext)|Omezuje množství textu, který může `CRichEditCtrl` uživatel zadat do objektu.|
|[CrichEditCtrl::LineFromchar](#linefromchar)|Určuje, který řádek obsahuje daný znak.|
|[CRichEditCtrl::LineIndex](#lineindex)|Načte index znaků daného řádku `CRichEditCtrl` v tomto objektu.|
|[CRichEditCtrl::Čárová délka](#linelength)|Načte délku daného řádku `CRichEditCtrl` v tomto objektu.|
|[CRichEditCtrl::LineScroll](#linescroll)|Posune text v `CRichEditCtrl` tomto objektu.|
|[CRichEditCtrl::Paste](#paste)|Vloží obsah schránky do tohoto rozšířeného ovládacího prvku úprav.|
|[CRichEditCtrl::PasteSpecial](#pastespecial)|Vloží obsah schránky do tohoto rozšířeného ovládacího prvku úprav v určeném datovém formátu.|
|[CRichEditCtrl::PosFromChar](#posfromchar)|Načte souřadnice klientské oblasti zadaného znaku v ovládacím prvku úprav.|
|[CRichEditCtrl::Znovu](#redo)|Znovu provádí další akci ve frontě opakování ovládacího prvku.|
|[CRichEditCtrl::ReplaceSel](#replacesel)|Nahradí aktuální výběr v `CRichEditCtrl` tomto objektu zadaným textem.|
|[CRichEditCtrl::RequestResize](#requestresize)|Vynutí, aby tento `CRichEditCtrl` objekt odeslal oznámení o změně velikosti požadavku.|
|[CRichEditCtrl::SetAutoURLDetect](#setautourldetect)|Označuje, zda je automatické zjišťování adres URL aktivní v ovládacím prvku rich edit.|
|[CRichEditCtrl::SetBackgroundColor](#setbackgroundcolor)|Nastaví barvu pozadí `CRichEditCtrl` v tomto objektu.|
|[CRichEditCtrl::SetDefaultCharFormat](#setdefaultcharformat)|Nastaví aktuální výchozí atributy formátování `CRichEditCtrl` znaků v tomto objektu.|
|[CRichEditCtrl::SetEventMask](#seteventmask)|Nastaví masku `CRichEditCtrl` události pro tento objekt.|
|[CRichEditCtrl::SetModify](#setmodify)|Nastaví nebo vymaže příznak změny pro tento `CRichEditCtrl` objekt.|
|[CRichEditCtrl::SetOLECallback](#setolecallback)|Nastaví `IRichEditOleCallback` objekt COM pro tento ovládací prvek s rozšířenými úpravami.|
|[CRichEditCtrl::SetOptions](#setoptions)|Nastaví volby `CRichEditCtrl` pro tento objekt.|
|[CRichEditCtrl::SetParaFormat](#setparaformat)|Nastaví atributy formátování odstavce v aktuálním `CRichEditCtrl` výběru v tomto objektu.|
|[CRichEditCtrl::SetPunctuation CRichEditCtrl::SetPunctuation CRichEditCtrl::SetPunctuation CRich](#setpunctuation)|Nastaví interpunkční znaménko pro ovládací prvek s bohatou úpravou. Tato zpráva je k dispozici pouze v asijských jazykových verzích operačního systému.|
|[CRichEditCtrl::SetReadOnly](#setreadonly)|Nastaví možnost jen pro `CRichEditCtrl` čtení pro tento objekt.|
|[CRichEditCtrl::SetRect](#setrect)|Nastaví formátovací obdélník `CRichEditCtrl` pro tento objekt.|
|[CRichEditCtrl::SetSel](#setsel)|Nastaví výběr `CRichEditCtrl` v tomto objektu.|
|[CRichEditCtrl::SetSelectionCharFormat](#setselectioncharformat)|Nastaví atributy formátování znaků v aktuálním `CRichEditCtrl` výběru v tomto objektu.|
|[CRichEditCtrl::SetTargetDevice](#settargetdevice)|Nastaví cílové výstupní `CRichEditCtrl` zařízení pro tento objekt.|
|[cRichEditCtrl::SetTextMode](#settextmode)|Nastaví textový režim nebo úroveň zrušení rozšířeného ovládacího prvku pro úpravy. Zpráva se nezdaří, pokud ovládací prvek obsahuje text.|
|[CRichEditCtrl::SetUndoLimit](#setundolimit)|Nastaví maximální počet akcí, které mohou být uloženy ve frontě vrátit.|
|[CRichEditCtrl::SetWordCharFormat](#setwordcharformat)|Nastaví atributy formátování znaků v aktuálním `CRichEditCtrl` slově v tomto objektu.|
|[CRichEditCtrl::SetWordWrapMode](#setwordwrapmode)|Nastaví možnosti zalamování a rozdělení slov pro ovládací prvek rich edit. Tato zpráva je k dispozici pouze v asijských jazykových verzích operačního systému.|
|[CRichEditCtrl::StopGroupTyping](#stopgrouptyping)|Zabrání ovládacímu prvku ve shromažďování dalších akcí zadávání do aktuální akce zpět. Ovládací prvek uloží další akci psaní, pokud existuje, do nové akce ve frontě zpět.|
|[cricheditctrl::Streamin](#streamin)|Vloží text ze vstupního `CRichEditCtrl` datového proudu do tohoto objektu.|
|[CRichEditCtrl::Vypoví](#streamout)|Ukládá text `CRichEditCtrl` z tohoto objektu do výstupního datového proudu.|
|[CRichEditCtrl::Zpět](#undo)|Obrátí poslední operaci úprav.|

## <a name="remarks"></a>Poznámky

"Rich edit control" je okno, ve kterém může uživatel zadávat a upravovat text. Text může být přiřazen k formátování znaků a odstavců a může obsahovat vložené objekty OLE. Ovládací prvky pro úpravy rich poskytují programovací rozhraní pro formátování textu. Aplikace však musí implementovat všechny součásti uživatelského rozhraní nezbytné k tomu, aby byly operace formátování dostupné uživateli.

Tento ovládací prvek Windows `CRichEditCtrl` Common (a tedy i třída) je k dispozici pouze pro programy spuštěné v systémech Windows 95/98 a Windows NT verze 3.51 a novějších. Třída `CRichEditCtrl` podporuje verze 2.0 a 3.0 rozšířeného ovládacího prvku Windows SDK.

> [!CAUTION]
> Pokud používáte rozšířený ovládací prvek pro úpravy v dialogovém okně (bez ohledu na to, zda je vaše aplikace SDI, MDI nebo dialogová okna), musíte před zobrazením dialogového okna zavolat [AfxInitRichEdit](application-information-and-management.md#afxinitrichedit) jednou. Typické místo pro volání této funkce je `InitInstance` v členské funkci programu. Není nutné volat pro pokaždé, když zobrazíte dialogové okno, pouze při prvním zobrazení. `AfxInitRichEdit` Pokud pracujete se společností . `CRichEditView`

Další informace o `CRichEditCtrl`používání naleznete v tématu:

- [Ovládací prvky](../../mfc/controls-mfc.md)

- [Používání atributu CRichEditCtrl](../../mfc/using-cricheditctrl.md)

Příklad použití rozšířeného ovládacího prvku pro úpravy v aplikaci knihovny MFC naleznete v ukázkové aplikaci [wordpadu.](../../overview/visual-cpp-samples.md)

## <a name="inheritance-hierarchy"></a>Hierarchie dědičnosti

[CObjekt](../../mfc/reference/cobject-class.md)

[CCmdCíl](../../mfc/reference/ccmdtarget-class.md)

[Cwnd](../../mfc/reference/cwnd-class.md)

`CRichEditCtrl`

## <a name="requirements"></a>Požadavky

**Záhlaví:** afxcmn.h

## <a name="cricheditctrlcanpaste"></a><a name="canpaste"></a>CricheditCtrl::CanPaste

Určuje, zda může ovládací prvek rich edit vložit zadaný formát schránky.

```
BOOL CanPaste(UINT nFormat = 0) const;
```

### <a name="parameters"></a>Parametry

*nFormát*<br/>
Formát dat schránky pro dotaz. Tento parametr může být jeden z předdefinovaných formátů schránky nebo hodnota vrácená [registerClipboardFormat](/windows/win32/api/winuser/nf-winuser-registerclipboardformatw).

### <a name="return-value"></a>Návratová hodnota

Nenulová, pokud lze vložit formát schránky; jinak 0.

### <a name="remarks"></a>Poznámky

Pokud *nFormat* je `CanPaste` 0, se pokusí libovolný formát aktuálně ve schránce.

Další informace naleznete [v tématu EM_CANPASTE](/windows/win32/Controls/em-canpaste) message and [RegisterClipboardFormat](/windows/win32/api/winuser/nf-winuser-registerclipboardformatw) v sadě Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#1](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_1.cpp)]

## <a name="cricheditctrlcanredo"></a><a name="canredo"></a>CRichEditCtrl::CanRedo

Určuje, zda fronta opakování obsahuje nějaké akce.

```
BOOL CanRedo() const;
```

### <a name="return-value"></a>Návratová hodnota

Nenulová, pokud fronta opakování obsahuje akce, jinak 0.

### <a name="remarks"></a>Poznámky

Chcete-li zjistit název operace ve frontě opakování, volejte [CRichEditCtrl::GetRedoName](#getredoname). Chcete-li znovu provést poslední operaci Zpět, zavolejte [znovu](#redo).

Další informace naleznete v [tématu EM_CANREDO](/windows/win32/Controls/em-canredo) sady Windows SDK.

## <a name="cricheditctrlcanundo"></a><a name="canundo"></a>CRichEditCtrl::CanUndo

Určuje, zda lze vrátit zpět poslední operaci úprav.

```
BOOL CanUndo() const;
```

### <a name="return-value"></a>Návratová hodnota

Nenulová, pokud lze poslední operaci úprav vrátit zpět voláním členské funkce [Zpět;](#undo) 0, pokud jej nelze vrátit zpět.

### <a name="remarks"></a>Poznámky

Další informace naleznete v [EM_CANUNDO](/windows/win32/Controls/em-canundo) sady Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#2](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_2.cpp)]

## <a name="cricheditctrlcharfrompos"></a><a name="charfrompos"></a>CRichEditCtrl::CharFromPos

Načte informace o znaku v bodě určeném parametrem *pt*.

```
int CharFromPos(CPoint pt) const;
```

### <a name="parameters"></a>Parametry

*Pt*<br/>
[CPoint](../../atl-mfc-shared/reference/cpoint-class.md) objekt obsahující souřadnice zadaného bodu.

### <a name="return-value"></a>Návratová hodnota

Index znaků založený na nule znaku, který je nejblíže zadanému bodu. Pokud zadaný bod je za poslední znak v ovládacím prvku, vrácená hodnota označuje poslední znak v ovládacím prvku.

### <a name="remarks"></a>Poznámky

Tato členská funkce pracuje s rozšířeným ovládacím prvkem úprav. Chcete-li získat informace pro ovládací prvek pro úpravy, zavolejte [CEdit::CharFromPos](../../mfc/reference/cedit-class.md#charfrompos).

Další informace naleznete [v tématu EM_CHARFROMPOS](/windows/win32/Controls/em-charfrompos) v sadě Windows SDK.

## <a name="cricheditctrlclear"></a><a name="clear"></a>CRichEditCtrl::Vymazat

Odstraní (vymaže) aktuální výběr (pokud existuje) v ovládacím prvku rich edit.

```cpp
void Clear();
```

### <a name="remarks"></a>Poznámky

Odstranění provedené `Clear` pomocí lze vrátit zpět voláním členské funkce [Zpět.](#undo)

Chcete-li odstranit aktuální výběr a umístit odstraněný obsah do schránky, zavolejte členskou funkci [Vyjmout.](#cut)

Další informace naleznete v [WM_CLEAR](/windows/win32/dataxchg/wm-clear) sady Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#3](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_3.cpp)]

## <a name="cricheditctrlcopy"></a><a name="copy"></a>CRichEditCtrl::Kopírovat

Zkopíruje aktuální výběr (pokud existuje) v ovládacím prvku rich edit do schránky.

```cpp
void Copy();
```

### <a name="remarks"></a>Poznámky

Další informace naleznete v [tématu WM_COPY](/windows/win32/dataxchg/wm-copy) sady Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#4](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_4.cpp)]

## <a name="cricheditctrlcreate"></a><a name="create"></a>CRichEditCtrl::Vytvořit

Vytvoří ovládací prvek windows rich edit `CRichEditCtrl` a přidruží jej k tomuto objektu.

```
virtual BOOL Create(
    DWORD dwStyle,
    const RECT& rect,
    CWnd* pParentWnd,
    UINT nID);
```

### <a name="parameters"></a>Parametry

*dwStyl*<br/>
Určuje styl ovládacího prvku pro úpravy. Použijte kombinaci stylů oken uvedených v části **Poznámky** níže a [upravte styly ovládacích prvků](/windows/win32/Controls/edit-control-styles)popsané v sadě Windows SDK.

*Rect*<br/>
Určuje velikost a umístění ovládacího prvku pro úpravy. Může být [CRect](../../atl-mfc-shared/reference/crect-class.md) objekt nebo [RECT](/windows/win32/api/windef/ns-windef-rect) struktury.

*pParentWnd*<br/>
Určuje nadřazené okno ovládacího prvku pro úpravy (často [CDialog).](../../mfc/reference/cdialog-class.md) Nesmí být null.

*Nid*<br/>
Určuje ID ovládacího prvku pro úpravy.

### <a name="return-value"></a>Návratová hodnota

Nenulová, pokud je inicializace úspěšná; jinak 0.

### <a name="remarks"></a>Poznámky

Objekt vytvoříte ve `CRichEditCtrl` dvou krocích. Nejprve zavolejte konstruktorcRichEditCtrl, pak volání [CRichEditCtrl](#cricheditctrl) `Create`, který vytvoří ovládací `CRichEditCtrl` prvek pro úpravy systému Windows a připojí jej k objektu.

Při vytváření rozšířeného ovládacího prvku pro úpravy pomocí této funkce je třeba nejprve načíst potřebnou knihovnu společných ovládacích prvků. Chcete-li načíst knihovnu, volejte globální funkci [AfxInitRichEdit](application-information-and-management.md#afxinitrichedit), která inicializuje společnou knihovnu ovládacích prvků. V procesu `AfxInitRichEdit` musíte zavolat pouze jednou.

Při `Create` spuštění odesílá systém Windows do ovládacího prvku úprav zprávy [WM_NCCREATE](../../mfc/reference/cwnd-class.md#onnccreate), [WM_NCCALCSIZE](../../mfc/reference/cwnd-class.md#onnccalcsize), [WM_CREATE](../../mfc/reference/cwnd-class.md#oncreate)a [WM_GETMINMAXINFO.](../../mfc/reference/cwnd-class.md#ongetminmaxinfo)

Tyto zprávy jsou ve výchozím nastavení zpracovány členy [OnNcCreate](../../mfc/reference/cwnd-class.md#onnccreate), [OnNcCalcSize](../../mfc/reference/cwnd-class.md#onnccalcsize), [OnCreate](../../mfc/reference/cwnd-class.md#oncreate)a [OnGetMinMaxInfo](../../mfc/reference/cwnd-class.md#ongetminmaxinfo) v `CWnd` základní třídě. Chcete-li rozšířit výchozí zpracování zpráv, odvodit třídu z `CRichEditCtrl`, přidejte mapu zpráv do nové třídy a přepsat výše uvedené funkce obsluhy zprávy. Přepište `OnCreate`, například provést potřebnou inicializaci pro novou třídu.

Použijte následující [styly oken](../../mfc/reference/styles-used-by-mfc.md#window-styles) u ovládacího prvku pro úpravy.

- WS_CHILD vždycky.

- WS_VISIBLE obvykle.

- WS_DISABLED zřídka.

- WS_GROUP Chcete-li seskupit ovládací prvky.

- WS_TABSTOP Chcete-li do pořadí tabulátorů zahrnout ovládací prvek pro úpravy.

Další informace o stylech oken naleznete v tématu [CreateWindow](/windows/win32/api/winuser/nf-winuser-createwindoww) v sadě Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#5](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_5.cpp)]

## <a name="cricheditctrlcreateex"></a><a name="createex"></a>CRichEditCtrl::CreateEx

Vytvoří ovládací prvek (podřízené okno) a `CRichEditCtrl` přidruží jej k objektu.

```
virtual BOOL CreateEx(
    DWORD dwExStyle,
    DWORD dwStyle,
    const RECT& rect,
    CWnd* pParentWnd,
    UINT nID);
```

### <a name="parameters"></a>Parametry

*dwExStyl*<br/>
Určuje rozšířený styl vytvářeného ovládacího prvku. Seznam rozšířených stylů systému Windows naleznete v parametru *dwExStyle* pro [createwindowex](/windows/win32/api/winuser/nf-winuser-createwindowexw) v sadě Windows SDK.

*dwStyl*<br/>
Určuje styl ovládacího prvku pro úpravy. Použijte kombinaci stylů oken uvedených v části **Poznámky** [v](#create) části Vytvořit a [upravit styly ovládacích prvků](/windows/win32/Controls/edit-control-styles), popsanou v sadě Windows SDK.

*Rect*<br/>
Odkaz na [rect](/windows/win32/api/windef/ns-windef-rect) strukturu popisující velikost a umístění okna, které mají být vytvořeny, v klientských souřadnicích *pParentWnd*.

*pParentWnd*<br/>
Ukazatel na okno, které je nadřazený ovládací prvek.

*Nid*<br/>
ID podřízeného okna ovládacího prvku.

### <a name="return-value"></a>Návratová hodnota

Nenulová, pokud je úspěšná; jinak 0.

### <a name="remarks"></a>Poznámky

Místo `CreateEx` toho `Create` použijte rozšířené styly systému Windows určené **předmluvou**rozšířeného stylu systému Windows WS_EX_ .

## <a name="cricheditctrlcricheditctrl"></a><a name="cricheditctrl"></a>CRichEditCtrl::CRichEditCtrl

Vytvoří `CRichEditCtrl` objekt.

```
CRichEditCtrl();
```

### <a name="remarks"></a>Poznámky

Pomocí [příkazu Vytvořit](#create) vytvořte ovládací prvek pro bohaté úpravy systému Windows.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#6](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_6.cpp)]

## <a name="cricheditctrlcut"></a><a name="cut"></a>CRichEditCtrl::Vyjmout

Odstranit (vyjme) aktuální výběr (pokud existuje) v ovládacím prvku rich edit a zkopíruje odstraněný text do schránky.

```cpp
void Cut();
```

### <a name="remarks"></a>Poznámky

Odstranění provedené `Cut` pomocí lze vrátit zpět voláním členské funkce [Zpět.](#undo)

Chcete-li odstranit aktuální výběr bez umístění odstraněného textu do schránky, zavolejte členskou funkci [Vymazat.](#clear)

Další informace naleznete v [tématu WM_CUT](/windows/win32/dataxchg/wm-cut) v sadě Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#7](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_7.cpp)]

## <a name="cricheditctrldisplayband"></a><a name="displayband"></a>CRichEditCtrl::DisplayBand

Zobrazí část obsahu ovládacího prvku RICH edit (text a položky OLE), jak bylo dříve formátováno [FormatRange](#formatrange).

```
BOOL DisplayBand(LPRECT pDisplayRect);
```

### <a name="parameters"></a>Parametry

*pDisplayRect*<br/>
Ukazatel na [objekt RECT](/windows/win32/api/windef/ns-windef-rect) nebo [CRect](../../atl-mfc-shared/reference/crect-class.md) určující oblast zařízení pro zobrazení textu.

### <a name="return-value"></a>Návratová hodnota

Nenulová, pokud je zobrazení formátovaného textu úspěšné, jinak 0.

### <a name="remarks"></a>Poznámky

Text a položky OLE jsou oříznuty do oblasti určené ukazatelem *pDisplayRect*.

Další informace naleznete v [tématu EM_DISPLAYBAND](/windows/win32/Controls/em-displayband) sady Windows SDK.

### <a name="example"></a>Příklad

  Viz příklad [cricheditctrl::FormatRange](#formatrange).

## <a name="cricheditctrlemptyundobuffer"></a><a name="emptyundobuffer"></a>CRichEditCtrl::EmptyUndoBuffer

Obnoví (vymazat) příznak vrátit tento rozšířený ovládací prvek úpravy.

```cpp
void EmptyUndoBuffer();
```

### <a name="remarks"></a>Poznámky

Ovládací prvek nyní nebude moci vrátit zpět poslední operaci úprav. Příznak zpět je nastaven vždy, když lze operaci v rámci rozšířeného ovládacího prvku upravit vrátit zpět.

Příznak vrátit se automaticky vymazána při každém volání členské funkce [CWnd](../../mfc/reference/cwnd-class.md) [SetWindowText](../../mfc/reference/cwnd-class.md#setwindowtext).

Další informace naleznete v [tématu EM_EMPTYUNDOBUFFER](/windows/win32/Controls/em-emptyundobuffer) sady Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#8](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_8.cpp)]

## <a name="cricheditctrlfindtext"></a><a name="findtext"></a>CRichEditCtrl::FindText

Vyhledá text v ovládacím prvku rich edit.

```
long FindText(
    DWORD dwFlags,
    FINDTEXTEX* pFindText) const;
```

### <a name="parameters"></a>Parametry

*dwFlags*<br/>
Seznam možných hodnot naleznete v *tématu wParam* in [EM_FINDTEXTEXT](/windows/win32/Controls/em-findtextex) v sadě Windows SDK.

*pFindText*<br/>
Ukazatel na strukturu [FINDTEXTEX](/windows/win32/api/richedit/ns-richedit-findtextexw) poskytující parametry pro hledání a vrácení rozsahu, kde byla nalezena shoda.

### <a name="return-value"></a>Návratová hodnota

Pozice znaku na základě nuly v další shodě; - 1, pokud nejsou žádné další zápasy.

### <a name="remarks"></a>Poznámky

Můžete vyhledávat nahoru nebo dolů nastavením správných parametrů rozsahu ve `FINDTEXTEX` struktuře [CHARRANGE](/windows/win32/api/richedit/ns-richedit-charrange) v rámci struktury.

Další informace naleznete [v tématu EM_FINDTEXTEX](/windows/win32/Controls/em-findtextex) zprávy a struktury [FINDTEXTEX](/windows/win32/api/richedit/ns-richedit-findtextexw) v sadě Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#9](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_9.cpp)]

## <a name="cricheditctrlfindwordbreak"></a><a name="findwordbreak"></a>CRichEditCtrl::FindWordBreak

Najde další konec slova před nebo za pozicí určenou *nStart*.

```
DWORD FindWordBreak(
    UINT nCode,
    DWORD nStart) const;
```

### <a name="parameters"></a>Parametry

*nKód*<br/>
Označuje akci, která má být vyvedena. Seznam možných hodnot naleznete v popisu *kódu* parametru v EM_FINDWORDBREAK v sadě Windows SDK.

*nStartovat*<br/>
Pozice znaku na základě nuly, ze kterého chcete spustit.

### <a name="return-value"></a>Návratová hodnota

Na základě parametru *nCode*. Další informace naleznete v [EM_FINDWORDBREAK](/windows/win32/Controls/em-findwordbreak) sady Windows SDK.

### <a name="remarks"></a>Poznámky

Tuto člennou funkci můžete použít k načtení informací o znaku na dané pozici.

## <a name="cricheditctrlformatrange"></a><a name="formatrange"></a>CRichEditCtrl::FormatRange

Formátuje rozsah textu v ovládacím prvku rich edit pro konkrétní zařízení.

```
long FormatRange(
    FORMATRANGE* pfr,
    BOOL bDisplay = TRUE);
```

### <a name="parameters"></a>Parametry

*Pfr*<br/>
Ukazatel na [formatrange](/windows/win32/api/richedit/ns-richedit-formatrange) strukturu, která obsahuje informace o výstupním zařízení. Hodnota NULL označuje, že informace uložené v mezipaměti v rámci ovládacího prvku rich edit mohou být uvolněny.

*bDisplej*<br/>
Označuje, zda má být text vykreslen. Pokud false, text je jen měří.

### <a name="return-value"></a>Návratová hodnota

Index posledního znaku, který se vejde do oblasti plus jeden.

### <a name="remarks"></a>Poznámky

Obvykle toto volání následuje volání [DisplayBand](#displayband).

Další informace naleznete [v tématu EM_FORMATRANGE](/windows/win32/Controls/em-formatrange) message and [FORMATRANGE](/windows/win32/api/richedit/ns-richedit-formatrange) structure in the Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#10](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_10.cpp)]

## <a name="cricheditctrlgetcharpos"></a><a name="getcharpos"></a>CRichEditCtrl::GetCharPos

Získá pozici (levý horní roh) daného znaku v rámci tohoto `CRichEditCtrl` objektu.

```
CPoint GetCharPos(long lChar) const;
```

### <a name="parameters"></a>Parametry

*lChar*<br/>
Nulový index znaku.

### <a name="return-value"></a>Návratová hodnota

Umístění levého horního rohu znaku určeného *lChar*.

### <a name="remarks"></a>Poznámky

Znak je určen zadáním jeho nulové hodnoty indexu. Pokud *lChar* je větší než index posledního znaku v tomto `CRichEditCtrl` objektu, vrácená hodnota určuje souřadnice pozice znaku těsně za poslední znak v tomto `CRichEditCtrl` objektu.

Další informace naleznete v [EM_POSFROMCHAR](/windows/win32/Controls/em-posfromchar) sady Windows SDK.

## <a name="cricheditctrlgetdefaultcharformat"></a><a name="getdefaultcharformat"></a>CRichEditCtrl::GetDefaultCharFormat

Získá výchozí atributy formátování znaků `CRichEditCtrl` tohoto objektu.

```
DWORD GetDefaultCharFormat(CHARFORMAT& cf) const;  DWORD GetDefaultCharFormat(CHARFORMAT2& cf) const;
```

### <a name="parameters"></a>Parametry

*Viz*<br/>
V první verzi ukazatel na `CHARFORMAT` strukturu, která obsahuje výchozí atributy formátování znaků.

Ve druhé verzi ukazatel na `CHARFORMAT2` strukturu, což je rozšíření rich edit `CHARFORMAT` 2.0 na strukturu, obsahující výchozí atributy formátování znaků.

### <a name="return-value"></a>Návratová hodnota

Datový `dwMask` člen *cf*. Zadal výchozí atributy formátování znaků.

### <a name="remarks"></a>Poznámky

Další informace naleznete v EM_GETCHARFORMAT `CHARFORMAT` zprávy `CHARFORMAT2` a struktury a v sadě Windows SDK.

### <a name="example"></a>Příklad

  Viz příklad pro [SetDefaultCharFormat](#setdefaultcharformat).

## <a name="cricheditctrlgeteventmask"></a><a name="geteventmask"></a>CRichEditCtrl::GetEventMask

Získá masku události `CRichEditCtrl` pro tento objekt.

```
long GetEventMask() const;
```

### <a name="return-value"></a>Návratová hodnota

Maska události `CRichEditCtrl` pro tento objekt.

### <a name="remarks"></a>Poznámky

Maska události určuje, které `CRichEditCtrl` zprávy oznámení objekt odesílá do nadřazeného okna.

Další informace naleznete v [tématu EM_GETEVENTMASK](/windows/win32/Controls/em-geteventmask) v sadě Windows SDK.

### <a name="example"></a>Příklad

  Viz příklad [cricheditctrl::SetEventMask](#seteventmask).

## <a name="cricheditctrlgetfirstvisibleline"></a><a name="getfirstvisibleline"></a>CRichEditCtrl::GetFirstVisibleLine

Určuje nejvyšší viditelnou `CRichEditCtrl` čáru v tomto objektu.

```
int GetFirstVisibleLine() const;
```

### <a name="return-value"></a>Návratová hodnota

Nulový index nejvyšší viditelné čáry `CRichEditCtrl` v tomto objektu.

### <a name="remarks"></a>Poznámky

Další informace naleznete [v tématu EM_GETFIRSTVISIBLELINE](/windows/win32/Controls/em-getfirstvisibleline) v sadě Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#11](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_11.cpp)]

## <a name="cricheditctrlgetiricheditole"></a><a name="getiricheditole"></a>Cricheditctrl::getiricheditole

Přistupuje `IRichEditOle` `CRichEditCtrl` k rozhraní pro tento objekt.

```
IRichEditOle* GetIRichEditOle() const;
```

### <a name="return-value"></a>Návratová hodnota

Ukazatel na rozhraní [IRichEditOle,](/windows/win32/api/richole/nn-richole-iricheditole) které lze `CRichEditCtrl` použít pro přístup k funkci OLE tohoto objektu; NULL, pokud rozhraní není přístupné.

### <a name="remarks"></a>Poznámky

Toto rozhraní slouží `CRichEditCtrl` k přístupu k funkci OLE tohoto objektu.

Další informace naleznete [v tématu EM_GETOLEINTERFACE](/windows/win32/Controls/em-getoleinterface) zprávy a rozhraní [IRichEditOle](/windows/win32/api/richole/nn-richole-iricheditole) v sadě Windows SDK.

## <a name="cricheditctrlgetlimittext"></a><a name="getlimittext"></a>CRichEditCtrl::GetLimitText

Získá omezení textu `CRichEditCtrl` pro tento objekt.

```
long GetLimitText() const;
```

### <a name="return-value"></a>Návratová hodnota

Aktuální limit textu v bajtech `CRichEditCtrl` pro tento objekt.

### <a name="remarks"></a>Poznámky

Limit textu je maximální množství textu v bajtů, který může přijmout ovládací prvek rich edit.

Další informace naleznete v [tématu EM_GETLIMITTEXT](/windows/win32/Controls/em-getlimittext) v sadě Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#12](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_12.cpp)]

## <a name="cricheditctrlgetline"></a><a name="getline"></a>CRichEditCtrl::GetLine

Načte řádek textu z `CRichEditCtrl` tohoto objektu.

```
int GetLine(
    int nIndex,
    LPTSTR lpszBuffer) const;

int GetLine(
    int nIndex,
    LPTSTR lpszBuffer,
    int nMaxLength) const;
```

### <a name="parameters"></a>Parametry

*nIndex*<br/>
Nulový index řádku, který chcete načíst.

*lpszBuffer*<br/>
Odkazuje na vyrovnávací paměť pro příjem textu. První slovo vyrovnávací paměti musí určit maximální počet bajtů, které lze zkopírovat do vyrovnávací paměti.

*nMaxLength*<br/>
Maximální počet znaků, které lze zkopírovat do *lpszBuffer*. Druhá forma `GetLine` umístí tuto hodnotu do prvního slova vyrovnávací paměti určené *lpszBuffer*.

### <a name="return-value"></a>Návratová hodnota

Počet znaků zkopírovaných do *lpszBuffer*.

### <a name="remarks"></a>Poznámky

Zkopírovaný řádek neobsahuje ukončující znak null.

> [!NOTE]
> Vzhledem k tomu, že první slovo vyrovnávací paměti ukládá počet znaků, které mají být zkopírovány, ujistěte se, že vyrovnávací paměť je alespoň 4 bajty dlouhé.

Další informace naleznete v [EM_GETLINE](/windows/win32/Controls/em-getline) sady Windows SDK.

### <a name="example"></a>Příklad

  Viz příklad pro [GetLineCount](#getlinecount).

## <a name="cricheditctrlgetlinecount"></a><a name="getlinecount"></a>CRichEditCtrl::GetLineCount

Načte počet řádků v `CRichEditCtrl` objektu.

```
int GetLineCount() const;
```

### <a name="return-value"></a>Návratová hodnota

Počet řádků v `CRichEditCtrl` tomto objektu.

### <a name="remarks"></a>Poznámky

Další informace naleznete v [tématu EM_GETLINECOUNT](/windows/win32/Controls/em-getlinecount) sady Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#13](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_13.cpp)]

## <a name="cricheditctrlgetmodify"></a><a name="getmodify"></a>CRichEditCtrl::GetModify

Určuje, zda byl `CRichEditCtrl` obsah tohoto objektu změněn.

```
BOOL GetModify() const;
```

### <a name="return-value"></a>Návratová hodnota

Nenulová, pokud `CRichEditCtrl` byl text v tomto objektu změněn; jinak 0.

### <a name="remarks"></a>Poznámky

Systém Windows udržuje interní příznak označující, zda byl změněn obsah ovládacího prvku rich edit. Tento příznak je vymazánpři prvním vytvoření ovládacího prvku pro úpravy a lze jej také vymazat voláním členské funkce [SetModify.](#setmodify)

Další informace naleznete v [tématu EM_GETMODIFY](/windows/win32/Controls/em-getmodify) v sadě Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#14](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_14.cpp)]

## <a name="cricheditctrlgetoptions"></a><a name="getoptions"></a>CRichEditCtrl::GetOptions

Načte možnosti aktuálně nastavené pro ovládací prvek rich edit.

```
UINT GetOptions() const;
```

### <a name="return-value"></a>Návratová hodnota

Kombinace aktuálních hodnot příznaku možnosti. Seznam těchto hodnot naleznete v parametru *fOptions* ve zprávě [EM_SETOPTIONS,](/windows/win32/Controls/em-setoptions) jak je popsáno v sadě Windows SDK.

## <a name="cricheditctrlgetparaformat"></a><a name="getparaformat"></a>CRichEditCtrl::GetParaFormat

Získá atributy formátování odstavce aktuálního výběru.

```
DWORD GetParaFormat(PARAFORMAT& pf) const;  DWORD GetParaFormat(PARAFORMAT2& pf) const;
```

### <a name="parameters"></a>Parametry

*Pf*<br/>
V první verzi ukazatel na strukturu [PARAFORMAT](/windows/win32/api/richedit/ns-richedit-paraformat) pro uložení atributů formátování odstavce aktuálního výběru.

Ve druhé verzi ukazatel na strukturu [PARAFORMAT2,](/windows/win32/api/richedit/ns-richedit-paraformat2) což je rozšíření struktury `PARAFORMAT` Rich Edit 2.0, které obsahuje výchozí atributy formátování znaků.

### <a name="return-value"></a>Návratová hodnota

Datový `dwMask` člen *pf*. Určuje atributy formátování odstavce, které jsou konzistentní v celém aktuálním výběru.

### <a name="remarks"></a>Poznámky

Pokud je vybrán více než jeden odstavec, *pf* obdrží atributy prvního vybraného odstavce. Vrácená hodnota určuje, které atributy jsou konzistentní v celém výběru.

Další informace naleznete v [EM_GETPARAFORMAT](/windows/win32/Controls/em-getparaformat) `PARAFORMAT` zprávy `PARAFORMAT2` a struktury a v sadě Windows SDK.

### <a name="example"></a>Příklad

  Viz příklad [cricheditctrl::SetParaFormat](#setparaformat).

## <a name="cricheditctrlgetpunctuation"></a><a name="getpunctuation"></a>CRichEditCtrl::GetPunctuation

Získá aktuální interpunkční znaky v ovládacím prvku rich edit.

```
BOOL GetPunctuation(
    UINT fType,
    PUNCTUATION* lpPunc) const;
```

### <a name="parameters"></a>Parametry

*Ftype*<br/>
Příznak typu interpunkce, jak je popsáno v parametru *fType* [EM_GETPUNCTUATION](/windows/win32/Controls/em-getpunctuation) v sadě Windows SDK.

*lpPunc*<br/>
Ukazatel na strukturu [interpunkce,](/windows/win32/api/richedit/ns-richedit-punctuation) jak je popsáno v sadě Windows SDK.

### <a name="return-value"></a>Návratová hodnota

Nenulová, pokud byla operace úspěšná, jinak 0.

### <a name="remarks"></a>Poznámky

Tato členská funkce je k dispozici pouze v asijských jazykových verzích operačního systému.

## <a name="cricheditctrlgetrect"></a><a name="getrect"></a>CRichEditCtrl::GetRect

Načte formátovací obdélník pro `CRichEditCtrl` tento objekt.

```cpp
void GetRect(LPRECT lpRect) const;
```

### <a name="parameters"></a>Parametry

*lpRect*<br/>
[CRect](../../atl-mfc-shared/reference/crect-class.md) nebo ukazatel na [RECT](/windows/win32/api/windef/ns-windef-rect) přijímat formátování obdélník `CRichEditCtrl` tohoto objektu.

### <a name="remarks"></a>Poznámky

Formátovací obdélník je ohraničující obdélník pro text. Tato hodnota je nezávislá `CRichEditCtrl` na velikosti objektu.

Další informace naleznete v [EM_GETRECT](/windows/win32/Controls/em-getrect) sady Windows SDK.

### <a name="example"></a>Příklad

  Viz příklad [limittextu](#limittext).

## <a name="cricheditctrlgetredoname"></a><a name="getredoname"></a>CRichEditCtrl::GetRedoName

Načte typ další dostupné akce ve frontě opakování, pokud existuje.

```
UNDONAMEID GetRedoName() const;
```

### <a name="return-value"></a>Návratová hodnota

Pokud je `GetRedoName` úspěšná, vrátí typ výčtu [UNDONAMEID](/windows/win32/api/richedit/ne-richedit-undonameid) označující typ další akce ve frontě opakování ovládacího prvku. Pokud je fronta opakování prázdná nebo pokud je akce opakování ve `GetRedoName` frontě neznámého typu, vrátí hodnotu 0.

### <a name="remarks"></a>Poznámky

Mezi typy akcí, které lze vrátit zpět nebo předělat, patří operace psaní, odstraňování, přetažení, vyjmutí a vložení. Tyto informace mohou být užitečné pro aplikace, které poskytují rozšířené uživatelské rozhraní pro operace Vrátit a znovu, jako je například rozevírací seznam redoable akce.

## <a name="cricheditctrlgetsel"></a><a name="getsel"></a>CRichEditCtrl::GetSel

Načte hranice aktuálního výběru `CRichEditCtrl` v tomto objektu.

```cpp
void GetSel(CHARRANGE& cr) const;

void GetSel(
    long& nStartChar,
    long& nEndChar) const;
```

### <a name="parameters"></a>Parametry

*Čr*<br/>
Odkaz na strukturu [CHARRANGE](/windows/win32/api/richedit/ns-richedit-charrange) pro příjem hranic aktuálního výběru.

*nStartChar*<br/>
Nulový index prvního znaku v aktuálním výběru.

*nEndChar*<br/>
Nulový index posledního znaku v aktuálním výběru.

### <a name="remarks"></a>Poznámky

Dvě formy této funkce poskytují alternativní způsoby, jak získat hranice pro výběr. Stručný popis těchto formulářů následuje:

- **GetSel(** `cr` **)** Tento formulář `CHARRANGE` používá strukturu `cpMin` `cpMax` s jeho a členy vrátit hranice.

- **GetSel(** `nStartChar` **,** `nEndChar` **)** Tento formulář vrátí hranice v parametrech *nStartChar* a *nEndChar*.

Výběr zahrnuje vše, pokud`cpMin` je začátek ( nebo *nStartChar*) 0 a konec (`cpMax` nebo *nEndChar*) je - 1.

Další informace naleznete [v tématu EM_EXGETSEL](/windows/win32/Controls/em-exgetsel) zprávy a [charrange struktury](/windows/win32/api/richedit/ns-richedit-charrange) v sadě Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#15](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_15.cpp)]

## <a name="cricheditctrlgetselectioncharformat"></a><a name="getselectioncharformat"></a>CRichEditCtrl::GetSelectionCharFormat

Získá atributy formátování znaků aktuálního výběru.

```
DWORD GetSelectionCharFormat(CHARFORMAT& cf) const;  DWORD GetSelectionCharFormat(CHARFORMAT2& cf) const;
```

### <a name="parameters"></a>Parametry

*Viz*<br/>
V první verzi ukazatel na strukturu [CHARFORMAT](/windows/win32/api/richedit/ns-richedit-charformata) pro příjem atributů formátování znaků aktuálního výběru.

Ve druhé verzi ukazatel na strukturu [CHARFORMAT2,](/windows/win32/api/richedit/ns-richedit-charformat2w) což je rozšíření rich `CHARFORMAT` edit 2.0 struktury pro příjem atributů formátování znaků aktuálního výběru.

### <a name="return-value"></a>Návratová hodnota

Datový `dwMask` člen *cf*. Určuje atributy formátování znaků, které jsou konzistentní v celém aktuálním výběru.

### <a name="remarks"></a>Poznámky

Parametr *cf* obdrží atributy prvního znaku v aktuálním výběru. Vrácená hodnota určuje, které atributy jsou konzistentní v celém výběru.

Další informace naleznete v [EM_GETCHARFORMAT](/windows/win32/Controls/em-getcharformat) `CHARFORMAT` zprávy `CHARFORMAT2` a struktury a v sadě Windows SDK.

### <a name="example"></a>Příklad

  Viz příklad pro [SetSelectionCharFormat](#setselectioncharformat).

## <a name="cricheditctrlgetselectiontype"></a><a name="getselectiontype"></a>CRichEditCtrl::GetSelectionType

Určuje typ výběru `CRichEditCtrl` v tomto objektu.

```
WORD GetSelectionType() const;
```

### <a name="return-value"></a>Návratová hodnota

Příznaky označující obsah aktuálního výběru. Kombinace následujících příznaků:

- SEL_EMPTY Označuje, že neexistuje žádný aktuální výběr.

- SEL_TEXT Označuje, že aktuální výběr obsahuje text.

- SEL_OBJECT Označuje, že aktuální výběr obsahuje alespoň jednu položku OLE.

- SEL_MULTICHAR Označuje, že aktuální výběr obsahuje více než jeden znak textu.

- SEL_MULTIOBJECT Označuje, že aktuální výběr obsahuje více než jeden objekt OLE.

### <a name="remarks"></a>Poznámky

Další informace naleznete v [tématu EM_SELECTIONTYPE](/windows/win32/Controls/em-selectiontype) sady Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#16](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_16.cpp)]

## <a name="cricheditctrlgetseltext"></a><a name="getseltext"></a>CRichEditCtrl::GetSelText

Načte text z aktuálního `CRichEditCtrl` výběru v tomto objektu.

```
long GetSelText(LPSTR lpBuf) const;  CString GetSelText() const;
```

### <a name="parameters"></a>Parametry

*lpBuf*<br/>
Ukazatel na vyrovnávací paměť pro příjem textu v aktuálním výběru.

### <a name="return-value"></a>Návratová hodnota

Záleží na formě:

- **GetSelText(** `lpBuf` **)** Počet znaků zkopírovaných do *lpBuf*, bez ukončení null.

- **GetSelText( )** Řetězec obsahující aktuální výběr.

### <a name="remarks"></a>Poznámky

Pokud použijete první formulář **GetSelText(** `lpBuf` **)**, musíte zajistit, aby vyrovnávací paměť byla dostatečně velká pro text, který obdrží. Volání [GetSel](#getsel) určit počet znaků v aktuálním výběru.

Další informace naleznete v [tématu EM_GETSELTEXT](/windows/win32/Controls/em-getseltext) v sadě Windows SDK.

### <a name="example"></a>Příklad

  Viz příklad [cricheditctrl::GetSelectionType](#getselectiontype).

## <a name="cricheditctrlgettextlength"></a><a name="gettextlength"></a>CRichEditCtrl::GetTextLength

Načte délku textu ve znacích `CRichEditCtrl` v tomto objektu, bez ukončujícího znaku null.

```
long GetTextLength() const;
```

### <a name="return-value"></a>Návratová hodnota

Délka textu v tomto `CRichEditCtrl` objektu.

### <a name="remarks"></a>Poznámky

Další informace naleznete v [WM_GETTEXTLENGTH](/windows/win32/winmsg/wm-gettextlength) sady Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#17](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_17.cpp)]

## <a name="cricheditctrlgettextlengthex"></a><a name="gettextlengthex"></a>CRichEditCtrl::GetTextLengthEx

Vypočítá délku textu v ovládacím prvku rich edit.

```
long GetTextLengthEx(
    DWORD dwFlags,
    UINT uCodePage = -1) const;
```

### <a name="parameters"></a>Parametry

*dwFlags*<br/>
Hodnota určující metodu, která má být použita při určování délky textu. Tento člen může být jedna nebo více hodnot uvedených v členu příznaků [GETTEXTLENGTHEX](/windows/win32/api/richedit/ns-richedit-gettextlengthex) popsaného v sadě Windows SDK.

*uCodePage*<br/>
Znaková stránka pro překlad (CP_ACP pro znakovou stránku ANSI, 1200 pro Unicode).

### <a name="return-value"></a>Návratová hodnota

Počet znaků nebo bajtů v ovládacím prvku úprav. Pokud byly v *dwFlags*nastaveny nekompatibilní příznaky , vrátí tato členská funkce E_INVALIDARG.

### <a name="remarks"></a>Poznámky

`GetTextLengthEx`poskytuje další způsoby určení délky textu. Podporuje funkci Rich Edit 2.0. Další informace naleznete v [tématu O ovládacích prvcích rozšířené úpravy](/windows/win32/Controls/about-rich-edit-controls) v sadě Windows SDK.

## <a name="cricheditctrlgettextmode"></a><a name="gettextmode"></a>CRichEditCtrl::GetTextMode

Načte aktuální textový režim a úroveň rozšířené úpravy.

```
UINT GetTextMode() const;
```

### <a name="return-value"></a>Návratová hodnota

Sada bitových příznaků z typu výčtu [TEXTMODE,](/windows/win32/api/richedit/ne-richedit-textmode) jak je popsáno v sadě Windows SDK. Příznaky označují aktuální textový režim a úroveň vrátit k bodu re-control.

## <a name="cricheditctrlgettextrange"></a><a name="gettextrange"></a>CRichEditCtrl::GetTextRange

Získá zadaný rozsah znaků.

```
int GetTextRange(
    int nFirst,
    int nLast,
    CString& refString) const;
```

### <a name="parameters"></a>Parametry

*nPrvní*<br/>
Index pozice znaků bezprostředně předcházející prvnímu znaku v oblasti.

*nPoslední*<br/>
Pozice znaku bezprostředně za posledním znakem v rozsahu.

*refString*<br/>
Odkaz na [CString](../../atl-mfc-shared/reference/cstringt-class.md) objekt, který obdrží text.

### <a name="return-value"></a>Návratová hodnota

Počet zkopírovaných znaků bez ukončujícího znaku null.

### <a name="remarks"></a>Poznámky

Další informace naleznete [v tématu EM_GETTEXTRANGE](/windows/win32/Controls/em-gettextrange) v sadě Windows SDK.

`GetTextRange`podporuje funkci Rich Edit 2.0. Další informace naleznete v [tématu O ovládacích prvcích rozšířené úpravy](/windows/win32/Controls/about-rich-edit-controls) v sadě Windows SDK.

## <a name="cricheditctrlgetundoname"></a><a name="getundoname"></a>CRichEditCtrl::GetUndoName

Načte typ další dostupné akce ve frontě zpět, pokud existuje.

```
UNDONAMEID GetUndoName() const;
```

### <a name="return-value"></a>Návratová hodnota

Pokud je akce zpět ve frontě zpět `GetUndoName` ovládacího prvku, vrátí typ výčtu [UNDONAMEID](/windows/win32/api/richedit/ne-richedit-undonameid) označující typ další akce ve frontě. Pokud je fronta zpět prázdná nebo pokud je akce zpět ve `GetUndoName` frontě neznámého typu, vrátí hodnotu 0.

### <a name="remarks"></a>Poznámky

Mezi typy akcí, které lze vrátit zpět nebo předělat, patří operace psaní, odstraňování, přetažení, vyjmutí a vložení. Tyto informace mohou být užitečné pro aplikace, které poskytují rozšířené uživatelské rozhraní pro operace Zpět a Znovu, jako je například rozevírací seznam akcí, které lze vrátit zpět.

## <a name="cricheditctrlgetwordwrapmode"></a><a name="getwordwrapmode"></a>CRichEditCtrl::GetWordWrapMode

Načte aktuální možnosti zalamování slov a rozdělení slov pro ovládací prvek rich edit.

```
UINT GetWordWrapMode() const;
```

### <a name="return-value"></a>Návratová hodnota

Aktuální možnosti zalamování slov a rozdělení slov. Tyto možnosti jsou popsány v [EM_SETWORDWRAPMODE](/windows/win32/Controls/em-setwordwrapmode) v sadě Windows SDK.

### <a name="remarks"></a>Poznámky

Tato členská funkce je k dispozici pouze pro asijské jazykové verze operačního systému.

## <a name="cricheditctrlhideselection"></a><a name="hideselection"></a>CRichEditCtrl::HideSelection

Změní viditelnost výběru.

```cpp
void HideSelection(
    BOOL bHide,
    BOOL bPerm);
```

### <a name="parameters"></a>Parametry

*bSkrýt*<br/>
Označuje, zda má být výběr zobrazen nebo skryt, pravda, aby se výběr skryl.

*bPerm*<br/>
Označuje, zda by tato změna viditelnosti pro výběr měla být trvalá.

### <a name="remarks"></a>Poznámky

Když *bPerm* je PRAVDA, změní ECO_NOHIDESEL `CRichEditCtrl` možnost pro tento objekt. Stručný popis této možnosti naleznete v tématu [SetOptions](#setoptions). Pomocí této funkce můžete nastavit všechny `CRichEditCtrl` volby pro tento objekt.

Další informace naleznete v [tématu EM_HIDESELECTION](/windows/win32/Controls/em-hideselection) v sadě Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#18](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_18.cpp)]

## <a name="cricheditctrllimittext"></a><a name="limittext"></a>cRichEditCtrl::LimitText

Omezuje délku textu, který může uživatel zadat do ovládacího prvku pro úpravy.

```cpp
void LimitText(long nChars = 0);
```

### <a name="parameters"></a>Parametry

*nChars*<br/>
Určuje délku (v bajtech) textu, který může uživatel zadat. Pokud je tento parametr 0 (výchozí hodnota), délka textu je nastavena na 64 kB bajtů.

### <a name="remarks"></a>Poznámky

Změna limitu textu omezí pouze text, který může uživatel zadat. Nemá žádný vliv na žádný text, který je již v ovládacím prvku pro úpravy, ani nemá `CWnd`vliv na délku textu zkopírovaného do ovládacího prvku upravit člennou funkcí [SetWindowText](../../mfc/reference/cwnd-class.md#setwindowtext) v aplikaci . Pokud aplikace používá `SetWindowText` funkci k umístění více textu do ovládacího `LimitText`prvku pro úpravy, než je určeno ve volání , může uživatel odstranit libovolný text v ovládacím prvku pro úpravy. Omezení textu však zabrání uživateli v nahrazení existujícího textu novým textem, pokud odstranění aktuálního výběru nezpůsobí, že text klesne pod textový limit.

> [!NOTE]
> Pro omezení textu se každá položka OLE počítá jako jeden znak.

Další informace naleznete v [tématu EM_EXLIMITTEXT](/windows/win32/Controls/em-exlimittext) sady Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#19](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_19.cpp)]

## <a name="cricheditctrllinefromchar"></a><a name="linefromchar"></a>CrichEditCtrl::LineFromchar

Načte číslo řádku, který obsahuje zadaný index znaků.

```
long LineFromChar(long nIndex) const;
```

### <a name="parameters"></a>Parametry

*nIndex*<br/>
Obsahuje hodnotu indexu na základě nuly pro požadovaný znak v textu ovládacího prvku úpravy nebo obsahuje -1. Pokud *nIndex* je -1, určuje aktuální řádek, to znamená řádek, který obsahuje stříška.

### <a name="return-value"></a>Návratová hodnota

Číslo řádku na základě nuly řádku obsahujícího index znaků určený *nIndex*. Pokud *nIndex* je -1, je vráceno číslo řádku, který obsahuje první znak výběru. Pokud není k dispozici žádný výběr, je vráceno aktuální číslo řádku.

### <a name="remarks"></a>Poznámky

Index znaků je počet znaků od začátku ovládacího prvku rich edit. Pro počítání znaků se položka OLE počítá jako jeden znak.

Další informace naleznete v [tématu EM_EXLINEFROMCHAR](/windows/win32/Controls/em-exlinefromchar) v sadě Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#20](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_20.cpp)]

## <a name="cricheditctrllineindex"></a><a name="lineindex"></a>CRichEditCtrl::LineIndex

Načte index znaků řádku v `CRichEditCtrl` rámci tohoto objektu.

```
int LineIndex(int nLine = -1) const;
```

### <a name="parameters"></a>Parametry

*nLinka*<br/>
Obsahuje hodnotu indexu pro požadovaný řádek v textu ovládacího prvku úpravy nebo obsahuje -1. Pokud *nLine* je -1, určuje aktuální řádek, to znamená řádek, který obsahuje stříška.

### <a name="return-value"></a>Návratová hodnota

Index znaků řádku určeného v *nLine* nebo -1, pokud je zadané číslo řádku větší, pak počet řádků v ovládacím prvku pro úpravy.

### <a name="remarks"></a>Poznámky

Index znaků je počet znaků od začátku ovládacího prvku rich edit k zadanému řádku.

Další informace naleznete v [tématu EM_LINEINDEX](/windows/win32/controls/em-lineindex) v sadě Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#21](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_21.cpp)]

## <a name="cricheditctrllinelength"></a><a name="linelength"></a>CRichEditCtrl::Čárová délka

Načte délku řádku v rozšířeném ovládacím prvku úprav.

```
int LineLength(int nLine = -1) const;
```

### <a name="parameters"></a>Parametry

*nLinka*<br/>
Určuje index znaků znaku v řádku, jehož délka má být načtena. Pokud je tento parametr -1, je vrácena délka aktuálního řádku (řádek, který obsahuje stříška), bez délky libovolného vybraného textu v řádku. Pokud `LineLength` je volána pro jednořádkový ovládací prvek úprav, tento parametr je ignorován.

### <a name="return-value"></a>Návratová hodnota

Když `LineLength` je volána pro víceřádkový ovládací prvek úprav, vrácená hodnota je délka (v TCHAR) řádku určeného *nLine*.  Neobsahuje znak carriage-return na konci řádku. Když `LineLength` je volána pro jednořádkový ovládací prvek úprav, vrácená hodnota je délka (v TCHAR) textu v ovládacím prvku upravit. Pokud nLine je větší než počet znaků v ovládacím prvku, vrácená hodnota je nula.

### <a name="remarks"></a>Poznámky

Členská funkce [LineIndex](#lineindex) slouží k načtení indexu znaků `CRichEditCtrl` pro dané číslo řádku v rámci tohoto objektu.

Další informace naleznete [v tématu EM_LINELENGTH](/windows/win32/Controls/em-linelength) v sadě Windows SDK.

### <a name="example"></a>Příklad

  Viz příklad pro [LineIndex](#lineindex).

## <a name="cricheditctrllinescroll"></a><a name="linescroll"></a>CRichEditCtrl::LineScroll

Posune text víceřádkového ovládacího prvku pro úpravy.

```cpp
void LineScroll(
    int nLines,
    int nChars = 0);
```

### <a name="parameters"></a>Parametry

*nŘádky*<br/>
Určuje počet řádků, které se mají posouvat svisle.

*nChars*<br/>
Určuje počet pozic znaků, které se mají posouvat vodorovně. Tato hodnota je ignorována, pokud má ovládací prvek rich edit ES_RIGHT nebo ES_CENTER stylu. [Styly úprav](../../mfc/reference/styles-used-by-mfc.md#edit-styles) jsou určeny v poli [Vytvořit](#create).

### <a name="remarks"></a>Poznámky

Ovládací prvek úpravse se neposune svisle za poslední řádek textu v ovládacím prvku úprav. Pokud aktuální řádek plus počet řádků určený *nLines* překročí celkový počet řádků v ovládacím prvku pro úpravy, hodnota se upraví tak, aby se poslední řádek ovládacího prvku pro úpravy posunul na začátek okna úprava-ovládací prvek.

`LineScroll`lze použít k vodorovnému posunu za poslední znak libovolného řádku.

Další informace naleznete [v tématu EM_LINESCROLL](/windows/win32/Controls/em-linescroll) v sadě Windows SDK.

### <a name="example"></a>Příklad

  Viz příklad [getfirstvisibleline](#getfirstvisibleline).

## <a name="cricheditctrlpaste"></a><a name="paste"></a>CRichEditCtrl::Paste

Vloží data ze schránky do `CRichEditCtrl` v místě vložení, umístění stříšky.

```cpp
void Paste();
```

### <a name="remarks"></a>Poznámky

Data se vkládají pouze v případě, že schránka obsahuje data v rozpoznaný formát.

Další informace naleznete v [tématu WM_PASTE](/windows/win32/dataxchg/wm-paste) v sadě Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#22](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_22.cpp)]

## <a name="cricheditctrlpastespecial"></a><a name="pastespecial"></a>CRichEditCtrl::PasteSpecial

Vloží data v určitém formátu schránky `CRichEditCtrl` do tohoto objektu.

```cpp
void PasteSpecial(
    UINT nClipFormat,
    DWORD dvAspect = 0,
    HMETAFILE hMF = 0);
```

### <a name="parameters"></a>Parametry

*nFormát klipu*<br/>
Formát schránky pro vložení `CRichEditCtrl` do tohoto objektu.

*dvAspect*<br/>
Aspekt zařízení pro data, která mají být načtena ze schránky.

*hMF*<br/>
Zpracovat metasoubor obsahující ikonické zobrazení objektu, který má být vložen.

### <a name="remarks"></a>Poznámky

Nový materiál je vložen v místě vložení, umístění stříšky.

Další informace naleznete v [EM_PASTESPECIAL](/windows/win32/Controls/em-pastespecial) sady Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#23](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_23.cpp)]

## <a name="cricheditctrlposfromchar"></a><a name="posfromchar"></a>CRichEditCtrl::PosFromChar

Načte souřadnice klientské oblasti zadaného znaku v ovládacím prvku úprav.

```
CPoint PosFromChar(UINT nChar) const;
```

### <a name="parameters"></a>Parametry

*Nchar*<br/>
Nula na základě indexu znaku.

### <a name="return-value"></a>Návratová hodnota

Pozice znaku (x, y). U jednořádkového ovládacího prvku pro úpravy je souřadnice y vždy nulová.

### <a name="remarks"></a>Poznámky

Další informace naleznete v [EM_POSFROMCHAR](/windows/win32/Controls/em-posfromchar) sady Windows SDK.

## <a name="cricheditctrlredo"></a><a name="redo"></a>CRichEditCtrl::Znovu

Znovu provádí další akci ve frontě opakování ovládacího prvku.

```
BOOL Redo();
```

### <a name="return-value"></a>Návratová hodnota

Nenulová, pokud je úspěšná; jinak 0.

### <a name="remarks"></a>Poznámky

Další informace naleznete v [tématu EM_REDO](/windows/win32/Controls/em-redo) v sadě Windows SDK.

## <a name="cricheditctrlreplacesel"></a><a name="replacesel"></a>CRichEditCtrl::ReplaceSel

Nahradí aktuální výběr v `CRichEditCtrl` tomto objektu zadaným textem.

```cpp
void ReplaceSel(
    LPCTSTR lpszNewText,
    BOOL bCanUndo = FALSE);
```

### <a name="parameters"></a>Parametry

*lpszNewText*<br/>
Ukazatel na řetězec s ukončeným hodnotou null obsahující náhradní text.

*bCanUndo*<br/>
Chcete-li určit, že tuto funkci lze vrátit zpět, nastavte hodnotu tohoto parametru na hodnotu TRUE. Výchozí hodnota je FALSE.

### <a name="remarks"></a>Poznámky

Chcete-li nahradit veškerý `CRichEditCtrl` text v tomto objektu, použijte [cWnd::SetWindowText](../../mfc/reference/cwnd-class.md#setwindowtext).

Pokud není k dispozici žádný aktuální výběr, je náhradní text vložen do kurzoru, tj.

Tato funkce naformátuje vložený text s existujícím formátováním znaků. Při nahrazování celého rozsahu `SetSel`textu (voláním (0,-1) před voláním `ReplaceSel`) je konec odstavce znak, který zachovává předchozí odstavec formátování, které v zděděné nově vložený text.

Další informace naleznete v [EM_REPLACESEL](/windows/win32/Controls/em-replacesel) sady Windows SDK.

### <a name="example"></a>Příklad

  Viz příklad pro [LineIndex](#lineindex).

## <a name="cricheditctrlrequestresize"></a><a name="requestresize"></a>CRichEditCtrl::RequestResize

Vynutí, aby tento `CRichEditCtrl` objekt odesílal EN_REQUESTRESIZE oznámení do nadřazeného okna.

```cpp
void RequestResize();
```

### <a name="remarks"></a>Poznámky

Tato funkce je užitečná během [CWnd::OnSize](../../mfc/reference/cwnd-class.md#onsize) zpracování pro bezedný `CRichEditCtrl` objekt.

Další informace naleznete v [EM_REQUESTRESIZE](/windows/win32/Controls/em-requestresize) zprávy a **bezedné rozšířené editační ovládací prvky** části [O rozšířené úpravy ovládacích prvků](/windows/win32/Controls/about-rich-edit-controls) v sadě Windows SDK.

## <a name="cricheditctrlsetautourldetect"></a><a name="setautourldetect"></a>CRichEditCtrl::SetAutoURLDetect

Nastaví ovládací prvek rich edit tak, aby automaticky detekoval adresu URL.

```
BOOL SetAutoURLDetect(BOOL bEnable = TRUE);
```

### <a name="parameters"></a>Parametry

*bEnable*<br/>
Určuje, zda je ovládací prvek nastaven tak, aby automaticky zjišťoval adresu URL. Pokud true, je povolena. Pokud false, je zakázáno.

### <a name="return-value"></a>Návratová hodnota

Nula, pokud je úspěšná, jinak nenulová. Například zpráva může selhat z důvodu nedostatku paměti.

### <a name="remarks"></a>Poznámky

Pokud je tato možnost povolena, prohledá ovládací prvek rich edit text a zjistí, zda odpovídá standardnímu formátu adresy URL. Seznam těchto formátů adres URL naleznete [v EM_AUTOURLDETECT](/windows/win32/Controls/em-autourldetect) sady Windows SDK.

> [!NOTE]
> Nenastavujte `SetAutoURLDetect` hodnotu TRUE, pokud ovládací prvek pro úpravy používá efekt CFE_LINK pro jiný text než adresy URL. `SetAutoURLDetect`povolí tento efekt pro adresy URL a zakáže jej pro všechny ostatní texty. Další informace o CFE_LINK efektu naleznete v [EN_LINK.](/windows/win32/Controls/en-link)

## <a name="cricheditctrlsetbackgroundcolor"></a><a name="setbackgroundcolor"></a>CRichEditCtrl::SetBackgroundColor

Nastaví barvu pozadí `CRichEditCtrl` pro tento objekt.

```
COLORREF SetBackgroundColor(
    BOOL bSysColor,
    COLORREF cr);
```

### <a name="parameters"></a>Parametry

*bSysBarva*<br/>
Označuje, zda má být barva pozadí nastavena na systémovou hodnotu. Pokud je tato hodnota TRUE, *cr* je ignorována.

*Čr*<br/>
Požadovaná barva pozadí. Používá se pouze v *případě, že bSysColor* je FALSE.

### <a name="return-value"></a>Návratová hodnota

Předchozí barva pozadí `CRichEditCtrl` pro tento objekt.

### <a name="remarks"></a>Poznámky

Barvu pozadí lze nastavit na systémovou hodnotu nebo na zadanou hodnotu [COLORREF.](/windows/win32/gdi/colorref)

Další informace naleznete [v tématu EM_SETBKGNDCOLOR](/windows/win32/Controls/em-setbkgndcolor) zprávy a [colorref](/windows/win32/gdi/colorref) struktury v sadě Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#24](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_24.cpp)]

## <a name="cricheditctrlsetdefaultcharformat"></a><a name="setdefaultcharformat"></a>CRichEditCtrl::SetDefaultCharFormat

Nastaví atributy formátování znaků pro nový `CRichEditCtrl` text v tomto objektu.

```
BOOL SetDefaultCharFormat(CHARFORMAT& cf);
BOOL SetDefaultCharFormat(CHARFORMAT2& cf);
```

### <a name="parameters"></a>Parametry

*Viz*<br/>
V první verzi ukazatel na strukturu [CHARFORMAT](/windows/win32/api/richedit/ns-richedit-charformata) obsahující nové výchozí atributy formátování znaků.

Ve druhé verzi ukazatel na strukturu [CHARFORMAT2,](/windows/win32/api/richedit/ns-richedit-charformat2w) což je rozšíření `CHARFORMAT` struktury Rich Edit 2.0, obsahující výchozí atributy formátování znaků.

### <a name="return-value"></a>Návratová hodnota

Nenulová, pokud je úspěšná; jinak 0.

### <a name="remarks"></a>Poznámky

Tato funkce změní pouze `dwMask` atributy určené členem *cf.*

Další informace naleznete v [EM_SETCHARFORMAT](/windows/win32/Controls/em-setcharformat) `CHARFORMAT` zprávy `CHARFORMAT2` a struktury a v sadě Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#25](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_25.cpp)]

## <a name="cricheditctrlseteventmask"></a><a name="seteventmask"></a>CRichEditCtrl::SetEventMask

Nastaví masku `CRichEditCtrl` události pro tento objekt.

```
DWORD SetEventMask(DWORD dwEventMask);
```

### <a name="parameters"></a>Parametry

*dwEventMask*<br/>
Nová maska události `CRichEditCtrl` pro tento objekt.

### <a name="return-value"></a>Návratová hodnota

Předchozí maska události.

### <a name="remarks"></a>Poznámky

Maska události určuje, které `CRichEditCtrl` zprávy oznámení objekt odesílá do nadřazeného okna.

Další informace naleznete v [tématu EM_SETEVENTMASK](/windows/win32/Controls/em-seteventmask) v sadě Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#26](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_26.cpp)]

## <a name="cricheditctrlsetmodify"></a><a name="setmodify"></a>CRichEditCtrl::SetModify

Nastaví nebo vymaže upravený příznak pro ovládací prvek úprav.

```cpp
void SetModify(BOOL bModified = TRUE);
```

### <a name="parameters"></a>Parametry

*bZměněno*<br/>
Hodnota TRUE označuje, že text byl změněn a hodnota FALSE označuje, že je nezměněn. Ve výchozím nastavení je nastaven upravený příznak.

### <a name="remarks"></a>Poznámky

Upravený příznak označuje, zda byl změněn text v ovládacím prvku úprav. Je automaticky nastavena vždy, když uživatel změní text. Jeho hodnota může být načtena pomocí [getmodify](#getmodify) členské funkce.

Další informace naleznete v [EM_SETMODIFY](/windows/win32/Controls/em-setmodify) sady Windows SDK.

### <a name="example"></a>Příklad

  Viz příklad [getmodify](#getmodify).

## <a name="cricheditctrlsetolecallback"></a><a name="setolecallback"></a>CRichEditCtrl::SetOLECallback

Poskytuje `CRichEditCtrl` tento `IRichEditOleCallback` objekt objekt použít pro přístup k prostředkům a informacím souvisejícím s OLE.

```
BOOL SetOLECallback(IRichEditOleCallback* pCallback);
```

### <a name="parameters"></a>Parametry

*zpětné volání*<br/>
Ukazatel na objekt [IRichEditOleCallback,](/windows/win32/api/richole/nn-richole-iricheditolecallback) který tento `CRichEditCtrl` objekt použije k získání prostředků a informací souvisejících s OLE.

### <a name="return-value"></a>Návratová hodnota

Nenulová, pokud je úspěšná; jinak 0.

### <a name="remarks"></a>Poznámky

Tento `CRichEditCtrl` objekt bude volat [IUnknown::AddRef](/windows/win32/api/unknwn/nf-unknwn-iunknown-addref) zvýšit počet využití pro objekt COM určené *pCallback*.

Další informace naleznete [v tématu EM_SETOLECALLBACK](/windows/win32/Controls/em-setolecallback) zprávy a rozhraní [IRichEditOleCallback](/windows/win32/api/richole/nn-richole-iricheditolecallback) v sadě Windows SDK.

## <a name="cricheditctrlsetoptions"></a><a name="setoptions"></a>CRichEditCtrl::SetOptions

Nastaví volby `CRichEditCtrl` pro tento objekt.

```cpp
void SetOptions(
    WORD wOp,
    DWORD dwFlags);
```

### <a name="parameters"></a>Parametry

*wOp*<br/>
Označuje typ operace. Jedna z následujících hodnot:

- ECOOP_SET Nastavte možnosti na ty, které jsou určeny *dwFlags*.

- ECOOP_OR Zkombinujte aktuální možnosti s možnostmi určenými *dwFlags*.

- ECOOP_AND Zachovat pouze aktuální možnosti, které jsou také určeny *dwFlags*.

- ECOOP_XOR Logicky exkluzivní OR aktuální možnosti s těmi, které jsou určeny *dwFlags*.

*dwFlags*<br/>
Rozšířené možnosti úprav. Hodnoty příznaku jsou uvedeny v části Poznámky.

### <a name="remarks"></a>Poznámky

Možnosti mohou být kombinací následujících hodnot:

- ECO_AUTOWORDSELECTION Automatický výběr slov poklepáním.

- ECO_AUTOVSCROLL Automaticky posouvá text doprava o 10 znaků, když uživatel zadá znak na konci řádku. Když uživatel stiskne klávesu ENTER, ovládací prvek posune veškerý text zpět na pozici nula.

- ECO_AUTOHSCROLL Automaticky posouvá text o jednu stránku nahoru, když uživatel stiskne klávesu ENTER na posledním řádku.

- ECO_NOHIDESEL Negovat výchozí chování ovládacího prvku pro úpravy. Výchozí chování skryje výběr, když ovládací prvek ztratí vstupní fokus a zobrazí výběr, když ovládací prvek obdrží vstupní fokus. Pokud zadáte ECO_NOHIDESEL, vybraný text se převrátí, i když ovládací prvek nemá fokus.

- ECO_READONLY Zabrání uživateli v psaní nebo úpravách textu v ovládacím prvku úprav.

- ECO_WANTRETURN Určuje, že návrat vozíku bude vložen, když uživatel stiskne klávesu ENTER při zadávání textu do víceřádkového ovládacího prvku pro úpravy rich v dialogovém okně. Pokud tento styl nezadáte, stisknutím klávesy ENTER odešlete příkaz do nadřazeného okna ovládacího prvku rich edit, které napodobuje klepnutí na výchozí tlačítko nadřazeného okna (například tlačítko OK v dialogovém okně). Tento styl nemá žádný vliv na jednořádkový ovládací prvek pro úpravy.

- ECO_SAVESEL Zachová výběr, když ovládací prvek ztratí fokus. Ve výchozím nastavení je při opětovném získání fokusu vybrán celý obsah ovládacího prvku.

- ECO_VERTICAL Nakreslí text a objekty ve svislém směru. K dispozici pouze pro asijské jazyky.

Další informace naleznete v [EM_SETOPTIONS](/windows/win32/Controls/em-setoptions) v sadě Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#27](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_27.cpp)]

## <a name="cricheditctrlsetparaformat"></a><a name="setparaformat"></a>CRichEditCtrl::SetParaFormat

Nastaví atributy formátování odstavce pro aktuální `CRichEditCtrl` výběr v tomto objektu.

```
BOOL SetParaFormat(PARAFORMAT& pf);
BOOL SetParaFormat(PARAFORMAT2& pf);
```

### <a name="parameters"></a>Parametry

*Pf*<br/>
V první verzi ukazatel na strukturu [PARAFORMAT](/windows/win32/api/richedit/ns-richedit-paraformat) obsahující nové výchozí atributy formátování odstavce.

Ve druhé verzi ukazatel na strukturu [PARAFORMAT2,](/windows/win32/api/richedit/ns-richedit-paraformat2) což je rozšíření struktury `PARAFORMAT` Rich Edit 2.0, které obsahuje výchozí atributy formátování znaků.

### <a name="return-value"></a>Návratová hodnota

Nenulová, pokud je úspěšná; jinak 0.

### <a name="remarks"></a>Poznámky

Tato funkce změní pouze `dwMask` atributy určené členem *pf.*

Další informace naleznete v [EM_SETPARAFORMAT](/windows/win32/Controls/em-setparaformat) `PARAFORMAT` zprávy `PARAFORMAT2` a struktury a v sadě Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#28](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_28.cpp)]

## <a name="cricheditctrlsetpunctuation"></a><a name="setpunctuation"></a>CRichEditCtrl::SetPunctuation CRichEditCtrl::SetPunctuation CRichEditCtrl::SetPunctuation CRich

Nastaví interpunkci v rozšířeném ovládacím prvku úprav.

```
BOOL SetPunctuation(
    UINT fType,
    PUNCTUATION* lpPunc);
```

### <a name="parameters"></a>Parametry

*Ftype*<br/>
Interpunkční příznak. Seznam možných hodnot naleznete v parametru *fType* pro [EM_SETPUNCTUATION](/windows/win32/Controls/em-setpunctuation) v sadě Windows SDK.

*lpPunc*<br/>
Ukazatel na strukturu [interpunkce,](/windows/win32/api/richedit/ns-richedit-punctuation) jak je popsáno v sadě Windows SDK.

### <a name="return-value"></a>Návratová hodnota

Nenulová, pokud je úspěšná, jinak 0.

### <a name="remarks"></a>Poznámky

Tato členská funkce je k dispozici pouze pro asijské jazykové verze operačního systému.

## <a name="cricheditctrlsetreadonly"></a><a name="setreadonly"></a>CRichEditCtrl::SetReadOnly

Změní možnost ECO_READONLY `CRichEditCtrl` pro tento objekt.

```
BOOL SetReadOnly(BOOL bReadOnly = TRUE);
```

### <a name="parameters"></a>Parametry

*bReadOnly*<br/>
Označuje, `CRichEditCtrl` zda by měl být tento objekt jen pro čtení.

### <a name="return-value"></a>Návratová hodnota

Nenulová, pokud je úspěšná; jinak 0.

### <a name="remarks"></a>Poznámky

Stručný popis této možnosti naleznete v tématu [SetOptions](#setoptions). Pomocí této funkce můžete nastavit všechny `CRichEditCtrl` volby pro tento objekt.

Další informace naleznete v [tématu EM_SETREADONLY](/windows/win32/Controls/em-setreadonly) v sadě Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#29](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_29.cpp)]

## <a name="cricheditctrlsetrect"></a><a name="setrect"></a>CRichEditCtrl::SetRect

Nastaví formátovací obdélník `CRichEditCtrl` pro tento objekt.

```cpp
void SetRect(LPCRECT lpRect);
```

### <a name="parameters"></a>Parametry

*lpRect*<br/>
[CRect](../../atl-mfc-shared/reference/crect-class.md) nebo ukazatel na [RECT,](/windows/win32/api/windef/ns-windef-rect) který označuje nové hranice pro formátování obdélníku.

### <a name="remarks"></a>Poznámky

Formátovací obdélník je omezující obdélník pro text. Omezující obdélník je nezávislý na velikosti okna ovládacího prvku rich edit. Při `CRichEditCtrl` prvním vytvoření tohoto objektu má formátovací obdélník stejnou velikost jako klientská oblast okna. Slouží `SetRect` k tomu, aby byl formátovací obdélník větší nebo menší než okno s rozšířenými úpravami.

Další informace naleznete v [EM_SETRECT](/windows/win32/Controls/em-setrect) sady Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#30](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_30.cpp)]

## <a name="cricheditctrlsetsel"></a><a name="setsel"></a>CRichEditCtrl::SetSel

Nastaví výběr `CRichEditCtrl` v rámci tohoto objektu.

```cpp
void SetSel(
    long nStartChar,
    long nEndChar);

void SetSel(CHARRANGE& cr);
```

### <a name="parameters"></a>Parametry

*nStartChar*<br/>
Nulový index prvního znaku pro výběr.

*nEndChar*<br/>
Nulový index posledního znaku pro výběr.

*Čr*<br/>
[CHARRANGE,](/windows/win32/api/richedit/ns-richedit-charrange) která obsahuje hranice aktuálního výběru.

### <a name="remarks"></a>Poznámky

Dvě formy této funkce poskytují alternativní způsoby nastavení hranice pro výběr. Stručný popis těchto formulářů následuje:

- **SetSel(** `cr` **)** Tento formulář `CHARRANGE` používá strukturu `cpMin` `cpMax` s jeho a členy nastavit hranice.

- **SetSel(** `nStartChar` **,** `nEndChar` **)** Tento formulář používá parametry *nStartChar* a *nEndChar* k nastavení hranic.

Stříška je umístěna na konci výběru označeného`cpMin` indexy začátek (`cpMax` nebo *nStartChar*) a end ( nebo *nEndChar*). Tato funkce posouvá obsah `CRichEditCtrl` tak, aby stříška je viditelná.

Chcete-li vybrat veškerý `CRichEditCtrl` text `SetSel` v tomto objektu, volání s počátečním indexem 0 a koncovým indexem - 1.

Další informace naleznete [v tématu EM_EXSETSEL](/windows/win32/Controls/em-exsetsel) zprávy a struktury [CHARRANGE](/windows/win32/api/richedit/ns-richedit-charrange) v sadě Windows SDK.

### <a name="example"></a>Příklad

  Viz příklad pro [GetSel](#getsel).

## <a name="cricheditctrlsetselectioncharformat"></a><a name="setselectioncharformat"></a>CRichEditCtrl::SetSelectionCharFormat

Nastaví atributy formátování znaků pro text v aktuálním výběru v tomto `CRichEditCtrl` objektu.

```
BOOL SetSelectionCharFormat(CHARFORMAT& cf);
BOOL SetSelectionCharFormat(CHARFORMAT2& cf);
```

### <a name="parameters"></a>Parametry

*Viz*<br/>
V první verzi ukazatel na strukturu [CHARFORMAT](/windows/win32/api/richedit/ns-richedit-charformata) obsahující nové atributy formátování znaků pro aktuální výběr.

Ve druhé verzi ukazatel na strukturu [CHARFORMAT2,](/windows/win32/api/richedit/ns-richedit-charformat2w) což je rozšíření `CHARFORMAT` struktury Rich Edit 2.0, obsahující nové atributy formátování znaků pro aktuální výběr.

### <a name="return-value"></a>Návratová hodnota

Nenulová, pokud je úspěšná; jinak 0.

### <a name="remarks"></a>Poznámky

Tato funkce změní pouze `dwMask` atributy určené členem *cf.*

Další informace naleznete [EM_SETCHARFORMAT](/windows/win32/Controls/em-setcharformat) v EM_SETCHARFORMAT `CHARFORMAT` `CHARFORMAT2` a struktury a v sadě Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#31](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_31.cpp)]

## <a name="cricheditctrlsettargetdevice"></a><a name="settargetdevice"></a>CRichEditCtrl::SetTargetDevice

Nastaví cílové zařízení a šířku čáry použité pro WYSIWYG (co `CRichEditCtrl` vidíte, je to, co získáte) formátování v tomto objektu.

```
BOOL SetTargetDevice(
    HDC hDC,
    long lLineWidth);

BOOL SetTargetDevice(
    CDC& dc,
    long lLineWidth);
```

### <a name="parameters"></a>Parametry

*Hdc*<br/>
Zpracovat kontext zařízení pro nové cílové zařízení.

*lŠířka čáry*<br/>
Šířka čáry, která se má použít pro formátování.

*Dc*<br/>
[CDC](../../mfc/reference/cdc-class.md) pro nové cílové zařízení.

### <a name="return-value"></a>Návratová hodnota

Nenulová, pokud je úspěšná; jinak 0.

### <a name="remarks"></a>Poznámky

Pokud je tato funkce úspěšná, ovládací prvek rich edit vlastní kontext zařízení předaný jako parametr. V takovém případě by volající funkce neměla zničit kontext zařízení.

Další informace naleznete v [EM_SETTARGETDEVICE](/windows/win32/Controls/em-settargetdevice) sady Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#32](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_32.cpp)]

## <a name="cricheditctrlsettextmode"></a><a name="settextmode"></a>cRichEditCtrl::SetTextMode

Nastaví režim textu nebo úroveň vrátit a znovu provést pro rozšířený ovládací prvek úprav.

```
BOOL SetTextMode(UINT fMode);
```

### <a name="parameters"></a>Parametry

*fMode*<br/>
Určuje nové nastavení textového režimu ovládacího prvku a parametrů úrovně zrušení. Seznam možných hodnot naleznete v parametru mode pro [EM_SETTEXTMODE](/windows/win32/Controls/em-settextmode) v sadě Windows SDK.

### <a name="return-value"></a>Návratová hodnota

Nula, pokud je úspěšná, jinak nenulová.

### <a name="remarks"></a>Poznámky

Popis režimů textu naleznete v EM_SETTEXTMODE v sadě Windows SDK.

Tato členská funkce se nezdaří, pokud ovládací prvek obsahuje text. Chcete-li se ujistit, že je ovládací prvek prázdný, odešlete [zprávu WM_SETTEXT](/windows/win32/winmsg/wm-settext) s prázdným řetězcem.

## <a name="cricheditctrlsetundolimit"></a><a name="setundolimit"></a>CRichEditCtrl::SetUndoLimit

Nastaví maximální počet akcí, které mohou být uloženy ve frontě vrátit.

```
UINT SetUndoLimit(UINT nLimit);
```

### <a name="parameters"></a>Parametry

*nLimit*<br/>
Určuje maximální počet akcí, které mohou být uloženy ve frontě vrátit. Chcete-li zakázat funkci Vrátit, nastavte nulu.

### <a name="return-value"></a>Návratová hodnota

Nový maximální počet akcí vrátit se vrátit k úpravě pro ovládací prvek rich edit.

### <a name="remarks"></a>Poznámky

Ve výchozím nastavení je maximální počet akcí ve frontě vrátit vrátit 100. Pokud toto číslo zvýšíte, musí být k dispozici dostatek paměti, aby se přizpůsobilo novému číslu. Pro lepší výkon nastavte limit na nejmenší možnou hodnotu.

## <a name="cricheditctrlsetwordcharformat"></a><a name="setwordcharformat"></a>CRichEditCtrl::SetWordCharFormat

Nastaví atributy formátování znaků pro aktuálně vybrané `CRichEditCtrl` slovo v tomto objektu.

```
BOOL SetWordCharFormat(CHARFORMAT& cf);
BOOL SetWordCharFormat(CHARFORMAT2& cf);
```

### <a name="parameters"></a>Parametry

*Viz*<br/>
V první verzi ukazatel na strukturu [CHARFORMAT](/windows/win32/api/richedit/ns-richedit-charformata) obsahující nové atributy formátování znaků pro aktuálně vybrané slovo.

Ve druhé verzi ukazatel na strukturu [CHARFORMAT2,](/windows/win32/api/richedit/ns-richedit-charformat2w) což je rozšíření struktury `CHARFORMAT` Rich Edit 2.0, obsahující nové atributy formátování znaků pro aktuálně vybrané slovo.

### <a name="return-value"></a>Návratová hodnota

Nenulová, pokud je úspěšná; jinak 0.

### <a name="remarks"></a>Poznámky

Tato funkce změní pouze `dwMask` atributy určené členem *cf.*

Další informace naleznete v [EM_SETCHARFORMAT](/windows/win32/Controls/em-setcharformat) `CHARFORMAT` zprávy `CHARFORMAT2` a struktury a v sadě Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#33](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_33.cpp)]

## <a name="cricheditctrlsetwordwrapmode"></a><a name="setwordwrapmode"></a>CRichEditCtrl::SetWordWrapMode

Nastaví možnosti zalamování a rozdělení slov pro ovládací prvek rich edit.

```
UINT SetWordWrapMode(UINT uFlags) const;
```

### <a name="parameters"></a>Parametry

*uPříznaky*<br/>
Možnosti nastavení pro zalamování a zalamování slov. Seznam možných možností naleznete v [EM_SETWORDWRAPMODE](/windows/win32/Controls/em-setwordwrapmode) sady Windows SDK.

### <a name="return-value"></a>Návratová hodnota

Aktuální možnosti zalamování a rozdělení slov.

### <a name="remarks"></a>Poznámky

Tato zpráva je k dispozici pouze v asijských jazykových verzích operačního systému.

## <a name="cricheditctrlstopgrouptyping"></a><a name="stopgrouptyping"></a>CRichEditCtrl::StopGroupTyping

Zabrání ovládacímu prvku ve shromažďování dalších akcí zadávání do aktuální akce zpět.

```cpp
void StopGroupTyping();
```

### <a name="remarks"></a>Poznámky

Ovládací prvek uloží další akci psaní, pokud existuje, do nové akce ve frontě zpět.

Další informace naleznete v [EM_STOPGROUPTYPING](/windows/win32/Controls/em-stopgrouptyping) sady Windows SDK.

## <a name="cricheditctrlstreamin"></a><a name="streamin"></a>cricheditctrl::Streamin

Nahradí text v `CRichEditCtrl` tomto objektu textem ze zadaného vstupního datového proudu.

```
long StreamIn(
    int nFormat,
    EDITSTREAM& es);
```

### <a name="parameters"></a>Parametry

*nFormát*<br/>
Příznaky určující formáty vstupních dat. Další informace naleznete v části Poznámky.

*es*<br/>
[EDITSTREAM](/windows/win32/api/richedit/ns-richedit-editstream) struktura určující vstupní datový proud. Další informace naleznete v části Poznámky.

### <a name="return-value"></a>Návratová hodnota

Počet znaků přečtených ze vstupního datového proudu.

### <a name="remarks"></a>Poznámky

Hodnota *nFormat* musí být jedna z následujících:

- SF_TEXT Označuje pouze čtení textu.

- SF_RTF Označuje čtení textu a formátování.

Některou z těchto hodnot lze kombinovat s SFF_SELECTION. Pokud je zadán `StreamIn` SFF_SELECTION, nahradí aktuální výběr obsahem vstupního datového proudu. Pokud není zadán, `StreamIn` nahradí celý obsah `CRichEditCtrl` tohoto objektu.

V `EDITSTREAM` parametrech *es*zadáte funkci zpětného volání, která vyplní vyrovnávací paměť textem. Tato funkce zpětného volání je volána opakovaně, dokud není vyčerpán vstupní datový proud.

Další informace naleznete [v tématu EM_STREAMIN](/windows/win32/Controls/em-streamin) zprávy a struktury [EDITSTREAM](/windows/win32/api/richedit/ns-richedit-editstream) v sadě Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#34](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_34.cpp)]

[!code-cpp[NVC_MFC_CRichEditCtrl#35](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_35.cpp)]

## <a name="cricheditctrlstreamout"></a><a name="streamout"></a>CRichEditCtrl::Vypoví

Zapíše obsah tohoto `CRichEditCtrl` objektu do zadaného výstupního datového proudu.

```
long StreamOut(
    int nFormat,
    EDITSTREAM& es);
```

### <a name="parameters"></a>Parametry

*nFormát*<br/>
Příznaky určující formáty výstupních dat. Další informace naleznete v části Poznámky.

*es*<br/>
[EDITSTREAM](/windows/win32/api/richedit/ns-richedit-editstream) struktura určující výstupní datový proud. Další informace naleznete v části Poznámky.

### <a name="return-value"></a>Návratová hodnota

Počet znaků zapsaných do výstupního datového proudu.

### <a name="remarks"></a>Poznámky

Hodnota *nFormat* musí být jedna z následujících:

- SF_TEXT Označuje pouze zápis textu.

- SF_RTF Označuje psaní textu a formátování.

- SF_RTFNOOBJS Označuje zápis textu a formátování a nahrazuje položky OLE mezerami.

- SF_TEXTIZED Označuje psaní textu a formátování s textovými reprezentacemi položek OLE.

Libovolnou z těchto hodnot lze kombinovat s SFF_SELECTION. Pokud je zadán `StreamOut` SFF_SELECTION, zapíše aktuální výběr do výstupního datového proudu. Pokud není zadán, `StreamOut` zapíše celý obsah `CRichEditCtrl` tohoto objektu.

V `EDITSTREAM` parametrech *es*zadáte funkci zpětného volání, která vyplní vyrovnávací paměť textem. Tato funkce zpětného volání je volána opakovaně, dokud není vyčerpán výstupní datový proud.

Další informace naleznete [v tématu EM_STREAMOUT](/windows/win32/Controls/em-streamout) zprávy a struktury [EDITSTREAM](/windows/win32/api/richedit/ns-richedit-editstream) v sadě Windows SDK.

### <a name="example"></a>Příklad

[!code-cpp[NVC_MFC_CRichEditCtrl#36](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_36.cpp)]

[!code-cpp[NVC_MFC_CRichEditCtrl#37](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_37.cpp)]

## <a name="cricheditctrlundo"></a><a name="undo"></a>CRichEditCtrl::Zpět

Zpět poslední operaci v ovládacím prvku rich edit.

```
BOOL Undo();
```

### <a name="return-value"></a>Návratová hodnota

Nenulová, pokud je operace zpět úspěšná; jinak 0.

### <a name="remarks"></a>Poznámky

Operaci zpět lze také vrátit zpět. Odstraněný text můžete například obnovit pomocí `Undo`prvního volání aplikace . Dokud nedochází k žádné operaci úprav, můžete text znovu odebrat `Undo`pomocí druhého volání .

Další informace naleznete v [tématu EM_UNDO](/windows/win32/Controls/em-undo) v sadě Windows SDK.

### <a name="example"></a>Příklad

  Viz příklad pro [CanUndo](#canundo).

## <a name="see-also"></a>Viz také

[MFC Ukázka WORDPAD](../../overview/visual-cpp-samples.md)<br/>
[CWnd – třída](../../mfc/reference/cwnd-class.md)<br/>
[Graf hierarchie](../../mfc/hierarchy-chart.md)<br/>
[CEdit – třída](../../mfc/reference/cedit-class.md)<br/>
[CRichEditView – třída](../../mfc/reference/cricheditview-class.md)
